# Inpa — Agent Guide (CLAUDE.md)

> **Inpa (인파) = Insure + Partner.** AI sales-support web app for individually-contracted insurance planners (원수사/GA 위촉직). Code ported/reused from `~/Desktop/foliio` (Foliio analysis edition).
> **This file is the development SSOT, written for the AI coding agent.** The human PM does NOT read it — they read `README.md` (Korean, PM-facing). Keep this file English + dense + current; keep README Korean + plain. PM communicates in Korean → reply to the PM in Korean even though this guide is English.

---

## 1. Current state (2026-07-04)

- **Phase 1, live in production.** Monorepo: `inpa_be/` (Django 4.2 + DRF, Python 3.11, 13 apps) + `inpa_fe/` (Next.js 16 + React 19 + TS + Tailwind, ~55 routes: public / auth / admin).
- **Deployed:** BE → Render (`https://inpa-be.onrender.com`, `/healthz/` → `{"status":"ok","service":"inpa-be"}`, DEBUG=False) · FE → Vercel (`https://in-pa.vercel.app`) · DB → Neon Postgres · email → Resend · CI = GitHub Actions. All $0. Render free tier **sleeps when idle** → first request slow.
- **Deploy workflow:** commit on the reused feat branch **`feat/benchmark-ui-revamp`** → PR to `master` → **merge auto-deploys** Vercel + Render. ⚠️ **Parallel sessions share this branch** — `git fetch` + `git log origin/master..HEAD` before pushing; PR numbers interleave (not strictly mine).
- **What works today:** email + Google auth · customer pipeline (4-stage **단계별** board + **목록** view, `status` axis, staleness cue) · OCR → coverage normalization → **traffic-light heatmap** · self-diagnosis inbound link (`/d`) · consent flows (customer-self) · **Calendly-style booking** (work hours → auto free slots → accept/decline) · personal schedule/calendar · dashboards (goals, retention, manager ROI) · 직업급수 search · script/talk library · share view (content-protected, `/s`) · notifications · admin back-office (usage tracking).
- **Closed behind env flags until legal review:** medical-history collection, §97 comparison-doc publishing, overseas (Claude API) transfer, paid quota (402 — `FREE_TIER_UNLIMITED=True` in beta).
- **Pre-launch sprint (2026-07-04, PR #59 pending merge):** full audit + 22-persona debate + scoring in `docs/prelaunch-review/` (5 files, SSOT for the launch backlog); launch-blocking fixes 1·2·4·5+6·7·8·9(code)·10·13(p1) implemented (BE **524 tests**). **Deploy follow-ups (Render Shell, once):** `repair_analysis_links --apply` + `cleanup_demo`; console: `JOB_RUNNER_TOKEN` (GH secret + Render env), Render Starter, backup cron install (`docs/dev/26`). **Deferred-to-last (PM):** legal identity fill, Plus price + 계좌이체 desk, Sentry DSN.

## 2. Context

- **Target users:** individually-contracted planners (sole proprietors) at carriers/GAs. Priority 1 = new planners (desperate for lead-gen); priority 2 = mid-career (management).
- **Value:** prospecting → coverage analysis → switch (갈아타기/승환) proposal in ONE flow. Analysis is the *start* of the sales action, not a report.
- **Differentiators:** (1) legitimize switching via an auto-generated 비교안내서 (comparison guide) as a §97 shield against improper-switching rules; (2) full 100+ coverage (담보) "frame" + per-carrier coverage-name normalization.
- **BM:** Freemium (all features open, free monthly quota, heavy users subscribe).
- **Org:** CPO = CTO (the user). No external legal counsel → compliance handled conservatively (safe defaults + public regulator/association guides), re-reviewed before paid launch.

## 3. Stack

- **FE:** Next.js 16 + React 19 + TypeScript + Tailwind v4. **Design tokens centralized in `app/globals.css`** (`:root` CSS vars → `@theme inline`; custom `@utility shadow-card`). ⚠️ `design/tokens/inpa-tokens.css` is a stale design reference, NOT the active source. **Nav = desktop left sidebar + mobile bottom tab** (`components/app-nav.tsx`; body push via `body:has(.app-sidebar) main{padding-left}`). Logos `design/logo/*.svg`.
- **BE:** Django 4.2 + DRF + Python 3.11. Reuses foliio's `core/ocr/claude_parser`, `customers/calculate.py` (8 cases, numpy_financial), coverage-normalization **verbatim** (re-porting risk avoidance = core asset).
- **DB:** PostgreSQL (prod = Neon free; local = SQLite). ORM-only, zero code impact, `psycopg2-binary`.
- **AI:** Anthropic Claude API. Model ids injected via env ONLY — `CLAUDE_MODEL_PARSE` (Opus: comparison/normalization), `CLAUDE_MODEL_BULK` (Haiku: bulk OCR), nightly = Batches. **NEVER hardcode model ids.**
- **Local-only dirs (gitignored, NEVER commit or quote):** `samples/` = real policy PDFs (PII/sensitive); `benchmark/` = UI reference screenshots; root `data/` = PM's raw data extracts (the operational copy lives under each app's `data/`). Root `*.jpeg`/`calender*.webp` are committed design refs. Root `landing_page.pdf` (untracked, 7.5MB) = PM's new.inpa.kr cinematic-landing 시안 (14p) — keep local, design SSOT for that project is the spec doc.

## 4. Commands

Monorepo — run BE commands in `inpa_be/`, FE in `inpa_fe/`.

### Backend (`inpa_be/`)
- Setup: `pip install -r requirements.txt` (venv). Default settings = `config.settings.local` (SQLite; `manage.py` sets it).
- Run: `python manage.py runserver` → :8000 (API root `/api/v1/`, health `/healthz/`).
- Migrations: `makemigrations` → `migrate`. **Pre-deploy check (required):** `python manage.py check`.
- Tests: all = `python manage.py test inpa`; app = `python manage.py test inpa.booking`; single = `python manage.py test inpa.accounts.tests.LoginTests.test_x`.
- Seeds (idempotent): `seed_demo` (demo data — manual only, NOT in deploy) · `seed_normalization` (coverage dict) · `seed_jobs` (직업급수 707-row `JobRiskCode` from `inpa/customers/data/job_risk_codes.json`; **bulk upsert + sync/prune**: deletes any `(sctg_cd,name)` not in the file → the file is the source of truth, so swapping the data file converges prod to exactly the file; `Customer.job_code` is SET_NULL so customers survive a swap) · `seed_promotion` (판촉물 카테고리 골격) · **`seed_billing`** (idempotent: `get_or_create` the `free` + `plus` Plans + backfill an active `free` Subscription for every user lacking one; **fixes `billing/signals.py` skipping subscription creation when the `free` Plan is absent** — price/limits are CREATE-time defaults only, so `get_or_create` preserves any later Django-Admin edits on redeploy) · **`seed_boards`** (real-service 공지 4 + FAQ 6; idempotent `get_or_create` keyed on title/question so Django-Admin edits survive redeploy; author = first admin, skips if no user) · `create_admin`. Render `startCommand` = `migrate → createcachetable → seed_normalization → seed_jobs → seed_promotion → seed_billing → seed_boards → gunicorn` (every deploy; `createcachetable` backs the DB cache for rate-limit throttling). **2026-07-04:** `seed_normalization` is now an identity-true natural-key **upsert** (PKs preserved; `admin_verified`/`ocr_learned` dict rows and existing analysis M2M links survive; no deletes without `--prune`), and both it and `seed_jobs` are **SeedMarker-guarded** (boot = no-op unless the in-code `SEED_VERSION` changed — see §7). Ops commands (manual): `cleanup_demo` (purge @inpa.local users + [DEMO] plans/shared rows, admin-protected) · `repair_analysis_links` (bare run = dry-run report, `--apply` = real relink) · `run_daily_jobs` (reminder engine + lead retention; normally fired daily 08:00 KST by `.github/workflows/daily-jobs.yml` → `POST /api/v1/jobs/run-daily/` with `X-JOB-TOKEN` == env `JOB_RUNNER_TOKEN`, unset = 404). Backup: `scripts/backup/` + `docs/dev/26-backup-restore-runbook.md` (PM's own server, nightly encrypted pg_dump).
- Django admin `/admin/`. Ops back-office = separate `admin_console` API + FE `/admin/*`.

### Frontend (`inpa_fe/`, Node 20)
- Setup: `npm ci`. Dev: `npm run dev` → :3000. Build: `npm run build` (**also typechecks — the ONLY FE gate; no FE test runner or lint script**). Prod: `npm start`.
- BE URL = `NEXT_PUBLIC_API_BASE` (fallback `localhost:8000/api/v1` + console warning if unset). **Build-time inlined → redeploy Vercel after changing the env var.**
- ⚠️ **Next 16 differs from training data** (`inpa_fe/AGENTS.md`): read `node_modules/next/dist/docs/` for the relevant guide before writing Next-API code.

### CI / Deploy (`.github/workflows/ci.yml`)
- On push/PR to main·master: (1) BE `check` + `test inpa`, (2) FE `npm ci` + `build`, (3) gitleaks secret scan.
- **Deploy is NOT CI** — Vercel(FE) + Render(BE, `render.yaml`) auto-deploy from GitHub; DB = Neon.

## 5. Architecture (big picture — spans multiple files)

### Request flow
Browser → Next page (`inpa_fe/app/**`) → **`inpa_fe/lib/api.ts`** (single BE-call gate: all endpoints+types in one file; admin in `lib/adminApi.ts`) → DRF `/api/v1/` (`config/urls.py` fans out to per-app `urls.py`) → ViewSet → model. Auth = DRF TokenAuthentication; token in FE `localStorage['inpa_token']` (`tokenStore`). Error bodies `{error|code|detail}` → normalized to FE `ApiError` (carries `.status`, `.code`, message).

### Multitenancy (visibility) — enforced in ONE place, never bypass
`inpa/core/mixins.py::OwnedQuerySetMixin` (filters to own rows + auto-injects owner on create) + `inpa/core/permissions.py::IsOwner`/`IsAdmin`/`IsEmailVerified`. **Owner-scoped ViewSets MUST attach this mixin + IsOwner.** Admin (`profile.is_admin`) bypasses read. Shared exceptions only: boards/notices/FAQ/promo-samples. SSOT: `docs/dev/02` §0 visibility matrix.

### Django app map (`inpa_be/inpa/`, 13 apps; `core` is a shared package)
- **`accounts`** — User (email PK), Profile, auth (signup / email-verify / login-lock / password-reset / password-change / withdrawal), Google (`google.py` social login, `google_calendar.py`), onboarding, manager dashboard (`manager.py`). Profile holds booking settings: `booking_msg_template`, `booking_default_duration`, `booking_buffer_min` (앞뒤 여유, default 60), `title` (직책).
- **`customers`** — customer CRUD; consent log (`ConsentLog`); customer-self public consent (`public_consent.py` `/c/<token>`); baseline presets (`presets.py`); contract disclosure checklist (`ContractChecklistItem`, `/customers/<id>/checklist/`).
  - **Two orthogonal axes:** `sales_stage` (db/contact/meeting/contract → shown **DB/TA/FA/청약**, the funnel position) ⟂ `status` (**active/hold/dormant/closed = 진행중/보류/휴면/종료**, the engagement state; default `active`). Only `active` customers get the staleness/무접촉 cue (subtle left-edge bar + 'N일 무접촉'); parked rows are dimmed + foldable via a '진행중만' toggle. `status` is NOT in SUBSTANTIVE (changing it does NOT bump `last_contacted_at`).
  - **D-Day auto-update** (`views.py::CustomerViewSet.perform_update`, `SUBSTANTIVE` set): a PATCH touching substantive fields (memo/info/`sales_stage`/churn…) bumps `last_contacted_at=now`; favorite/pin/`status` toggles do NOT. **PM decision: a 단계별 stage move IS an action → `sales_stage` is in SUBSTANTIVE.**
  - **`fa_reached_at`** (`models.py::save()` hook): timestamp the FIRST time `sales_stage` becomes `meeting`(FA); never overwritten → re-entry NOT recounted. Drives the dashboard "이번 달 미팅" metric.
  - **`compute_insurance_age`** (models.py): 보험나이 = 만 나이 + (직전 생일로부터 6개월 이상이면 +1). FE mirrors this live (`computeInsuranceAge` in the customer detail).
  - **`JobRiskCode`** = 직업급수 global master (shared, read-only; **707 메리츠 jobs** via `seed_jobs`; fields: name/alt_name/risk_grade 1·2·3·9/synonym(`|`-joined)/description/kidi_cd). `Customer.job_code` FK (SET_NULL). Search: `GET /api/v1/jobs/search/?q=` (`JobSearchView`, substring on name+alt+synonym+kidi, relevance-ranked; mounted top-level `jobs/search/`). FE: pick a job → 직업급수 auto-fills (registration modal **and** customer-detail InfoTab).
- **`insurances`** — insurance/coverage (owner-scoped via `customer__owner`); churn radar (`churn.py`, `is_cancelled`/`cancelled_at` → retention); public self-diagnosis (`self_diagnosis.py` `/d/<ref>`); OCR cross-verify (`verify.py`); manual ("직접 입력") insurance entry. **OCR upload (`/customers/<id>/insurances/ocr/`) is consent-gated**: 412 if `consent_overseas_at` is null (FE opens the consent modal before the file-pick, so a planner never hits a raw error).
- **`analysis`** — **standard coverage tree + per-carrier coverage-name normalization dict (shared global master)**. Calc engine `calculate.py` (heatmap); switch `compare.py`+`switch_verdict.py` (KEEP/SWITCH/NEUTRAL = planner-internal ONLY, never shown to customer per §97). Core foliio-ported asset. **갱신/비갱신 요금 분리(2026-07-02):** 보험료를 갱신형(`payment_period_type=3`)/비갱신형/적립으로 **표·절대수치**로 분리 노출 — `insurances/serializers.py::CaseFeeSerializer`(담보별)·`InsuranceFeeSerializer`(보험별+`case_fees`) → 히트맵 응답 `insurances` + `compare.py::_aggregate_side` 6분리필드(전부 null=`None` 미상). 판정·등급·색 **0**(사실만). FE `components/premium-split.tsx`.
  - **Heatmap UI** (`components/heatmap.tsx`): **traffic-light — 넉넉=green / 적정=yellow / 부족=red**. The '중립' status/chip/legend is **REMOVED** — when no baseline (`mode='neutral'`) the heatmap shows held amounts only (no grade color/label) + a clickable **'기준 설정하기' CTA** → `/settings/baseline` (never the word '중립'). 2-column category grid for scannability. **BE grading authority + honesty redline UNCHANGED** (no false grading without a baseline — the FE just hides grades & nudges to set one).
- **`booking`** — Calendly-style meeting booking (**full engine**). **`WorkHour`** (recurring weekly work hours, KST wall-clock) → **`availability.py::generate_available_slots`** computes free slots = work hours − meetings(±`Profile.booking_buffer_min`) − schedule blocks (recur + single, time-overlap). Public `/b/<token>` GET returns computed slots (identified by `start_at`, **NOT** a stored slot row); POST creates a **PENDING** `Meeting` (Profile-row `select_for_update` lock + `is_slot_available` recheck → 409 conflict w/ '담당 설계사와 상의' warning) + planner `meeting_booked` notification (carries `meeting` FK). Planner **accept** (`/meetings/<id>/accept/`) → confirmed + Google Calendar event (created here, not at booking time); **decline** (`/decline/`) → frees the time. `Meeting.status` = pending/confirmed/canceled/declined. Pending visible in the bell + the **/schedule '예약 요청' card**. `{소속직책}` merge field = `affiliation`+`title`. Legacy manual `MeetingSlot` **API removed (2026-07-02)** — model + `Meeting.slot` FK + admin kept (no destructive migration); availability is WorkHour-only. ★ **Timezone:** all overlap math in KST aware (UTC meetings/events → `localtime`; WorkHour/recur-block TimeFields = KST wall-clock → combine with KST date).
- **`schedule`** — personal schedule/todo/recurring-block (`ScheduleItem`, owner-scoped, FE `/schedule`). **Separate from `booking`** — the calendar just draws both. `kind` (event/todo/block = behavior) ⟂ `category` (5 types = color/legend: 고객미팅 / 생일·기념일 / 만기·갱신 / 업무 / 기타) + yearly-recurring birthday/anniversary (`anniversary_md`). ⚠️ **TIMEZONE rule:** single start/end = stored UTC shown KST / recurring-block `recur_*_time` = KST wall-clock stored as-is (**NEVER convert** — shifts 9h) / all_day & timeless todo = stored KST noon.
- **`dashboard`** — monthly goal (manual) + actuals (`aggregation.py`), expected-salary multiplier. **`compute_actuals` meetings = distinct customers who first reached FA this month** (`Customer.fa_reached_at`), NOT `booking.Meeting`; premium = this-month registered-policy monthly-premium sum; new_customers = this-month created. Retention 1/2/3yr (`compute_retention`; 0 cancellations → `has_cancellation_data=false`) + manager team aggregation (`accounts/manager.py`, no PII, ROI labeled "estimate"). **Both the home calendar and the /schedule calendar use the SAME 5-category legend** (고객미팅/생일·기념일/만기·갱신/업무/기타), colored by `ScheduleCategory`; the 알림 category is removed from both (alerts live in the top-right bell only).
- **`notifications`** (alerts+reminders, incl. promo/digital-asset + `meeting_booked` types; `Notification.meeting` FK powers accept/decline) · **`billing`** (plans + usage limits; 402 over-limit via `credit.py`; **admin-issued 1-month coupon**: `Coupon`/`CouponRedemption` + `POST /billing/coupons/redeem/`, `credit.py` expiry-aware) · **`boards`** (board/notice/FAQ/inquiry, mixed visibility) · **`promotion`** (promo orders + digital assets: 1 free download → 2nd+ → admin queue; category skeleton via `seed_promotion`) · **`admin_console`** (IsAdmin back-office; usage tracking, demo accounts excluded, FE `/admin/usage`) · **`analytics`** (north-star event tracking).
- **Security hardening:** DRF rate-limit throttling backed by Django **DB cache** (`createcachetable` in startCommand) — anti DoS / cost-bomb (OCR/AI) / brute-force. **Content protection** = FE `components/content-guard.tsx` on the share view (`/s`): watermark + copy-block + image-save deterrence (deterrent, not real DRM).
- **`core`** — shared mixins/permissions + `core/ocr/` (foliio vendored: `claude_parser.py`, `ocrparsing.py`, `ocrdata.py` carrier codes).

### OCR → analysis pipeline (the core foliio-reuse flow)
Policy PDF upload (`POST /customers/<id>/insurances/ocr/`) → `core/ocr/claude_parser` (pdfplumber + Claude Opus; `ANTHROPIC_API_KEY`-gated, 503 if unset; `max_tokens=8192`) → `CustomerInsuranceDetail` (M2M to standard tree) → `analysis/calculate.py` sums `held_amount` per leaf → if a `PlannerBaseline` matches → `graded` (부족/적정/넉넉); else `neutral` → heatmap/share view.
Normalization SSOT: `core/ocr/ocrparsing.py::COVERAGE_KEYWORDS` — ONE dict shared by both pipelines. Matching = **substring (`if kw in name`) + longest-keyword-first**. Fix mis-matches in dict DATA, not logic (see Gotchas).

### Public (unauthenticated) token endpoints — 1:1 with FE routes
`/s/<token>` (share view) · `/b/<token>` (booking) · `/c/<token>` (customer-self consent) · `/d/<ref>` (self-diagnosis) · `/p/<refcode>` (planner intro card — GET card, POST '상담 신청' → db lead `lead_source='introduction'`; `accounts/public.py::IntroductionCardView`, mounted in config/urls). All use TimestampSigner/ref_code tokens + ScopedRateThrottle. FE at `app/s|b|c|d|p/[token]/`.

### Settings & feature gates (`settings/base.py` — env-controlled, NEVER bypass in code)
- Env split: `local` (SQLite, DEBUG, console email, `/media/` local) ↔ `prod` (Postgres `DATABASE_URL`, whitenoise, security headers, Sentry; **fail-loud if SECRET_KEY unset**).
- Media uploads — **no AWS required**, 3 prod modes by priority: (1) S3-compatible (`AWS_*`; **Cloudflare R2** free recommended; private signed URLs protect PII); (2) Render persistent disk (`MEDIA_DISK_PATH`, paid); (3) local-temp (unset — lost on redeploy). Local dev serves `/media/`. **Prod = Cloudflare R2 (ACTIVE, PM-confirmed 2026-07-01): `Customer.business_card` + `Profile.profile_image` (planner photo) persist there; any new `ImageField`/`FileField` auto-uses the same default storage.**
- **Compliance gates (default CLOSED):** `COMPARE_AI_ENABLED` · `COMPARE_PUBLISH_ENABLED` (§97 hard-block pre-legal) · `ANALYZE_MEDICAL_ENABLED` · `REQUIRE_CUSTOMER_SELF_CONSENT`.
- **Feature flags:** `FREE_TIER_UNLIMITED` (beta quota bypass — currently True, 402 never fires) · `BOOKING_ENABLED` · `OCR_VERIFY_ENABLED` · `GOOGLE_OAUTH_*` (unset = feature hidden).

## 6. Conventions & redlines

- **★ Easy-words · positive-framing · audience-split (STANDING — the product's voice; applies to ALL planning AND code).** Three parts, never bypass:
  - **(a) Easy words for the planner (설계사).** Plain everyday Korean; never jargon/feature-words in rendered UI. '수기 입력'→'직접 입력'; 'OCR'→'증권 스캔/자동 정리'; 'Kanban/칸반'→'단계별'; 'list/리스트'→'목록'; 'lead/리드'→'고객'. If a planner would have to decode the word, rewrite it.
  - **(b) Audience split.** Risk/ops concepts (책임 회피, 법적 리스크, 컴플라이언스, §97, 부당승환, 국외이전 '게이트', planner_attested, 환수…) live ONLY in planner-internal boxes / this dev doc. **Customer-facing surfaces (`/s` · `/d` · `/c` · `/b`) show ONLY benefit + the next action** — never the legal machinery.
  - **(c) Positive guidance, never negative/not-yet/beta-sounding.** Forbidden in rendered copy: '안 됩니다', '못 합니다', '불가', '준비 중', '아직 없음', '자동 발송 없음', red rejection for an expected state. Reframe every block as the NEXT step (consent-gated upload → "고객 동의를 먼저 보내면 바로 분석할 수 있어요", not "동의 없이는 분석 불가").
- **★ Status-first persuasive voice (STANDING — PM 2026-07-03; memory `copy-philosophy-superiority`).** Korean planners compare their POSITION relative to peers, not products; persuasive copy should let the reader feel prepared/ahead, subtly. Wording rules for rendered persuasive copy (landing, pricing, upgrade modal, /d /p CTA blurbs, promotion, onboarding): 비용→**투자** · 싸다/저렴→**부담이 낮다** · '(제품이) 좋습니다'→**'이런 분에게 딱 맞습니다'** (fit framing) · '문의 주세요'→**'내 상황에 맞는지 먼저 확인해보세요'** (self-qualification CTA). EXEMPT: functional labels/buttons/errors, legal/consent wording. Guardrails: no fabricated superiority stats (honesty redline), never overdone (PM: "오글거리면 안 됨"), customer-facing surfaces keep benefit+next-action only (rule b).
- **NO em-dash (`—`, U+2014) in user-facing copy** — PM finds it "AI-sounding." Use comma / period / colon / parentheses / a Korean particle. No-value placeholder = `-` (hyphen), never `—`. Middle-dot `·`, hyphen `-`, en-dash `–` are fine. Code comments are exempt. When sweeping, only touch rendered strings.
- **Honesty redlines (disclaimers CONSOLIDATED):** no "reviewed/safe" badges (warranty liability). The "AI 초안 / 인파는 보험 중개 안 함" disclaimer exists ONLY at official spots — signup/onboarding, 약관, data-policy, and customer-facing AI output (`/s`, `/d`, AI 비교초안 box) — as ONE brief line each. Do NOT sprinkle it on every card. Unified wording: *"인파는 보험을 중개·권유하지 않는 분석·정리 소프트웨어입니다. 보장 판단과 고객 안내는 설계사님의 업무이며, 산출물은 AI가 정리한 참고 자료입니다."*; customer short form: *"인파가 등록된 보장 정보를 정리한 참고 자료입니다."* Tone = factual ("중개 안 하는 도구"), NOT blame ("모든 책임은 설계사").
- **Theme guardrail:** service pages = **light-fixed**; dark mode = **admin only** (`app/admin/layout.tsx` `.theme-system`). NEVER add `dark:` variants to service screens.
- **Copy:** product UI = plain language, NO `§`/legal-clause notation in service copy. (Internal/planner-only boxes may reference §97.)
- **Git:** Conventional Commits (Korean scope ok, e.g. `feat(동의)`). Small per-feature commits; don't mix refactor + feature. Commit only when the user asks. Branch before working on the default branch.
- **Docs upkeep (standing rule):** whenever a feature is implemented AND merged AND deployed, UPDATE both `README.md` (Korean, PM-facing) and this `CLAUDE.md` (sections above + the changelog). Do it as the closing step of a deploy, unprompted.

## 7. Gotchas (read BEFORE touching the area — these have bitten)

- **`update_fields` + model `save()` hook**: when a `save(update_fields=[...])` is used but the model's `save()` hook sets ANOTHER field (e.g. `Customer.save()` stamps `fa_reached_at` on stage→meeting), that field is NOT written unless it's in `update_fields`. Booking auto-promote includes BOTH (`['sales_stage','fa_reached_at']`). Bit once (2026-06-29). Same trap applies to any hook-derived field.
- **Email verification = BE self-contained token** (`accounts`): `signing.dumps(pk)` → link and verify use `token` ONLY, no `uid` (UNLIKE reset-password). **If FE requires `uid`, signup is fully blocked — do NOT regress.** Resend button exists.
- **Consent token is multi-scope** (`customers/tokens.py`): `make_consent_token(customer, scopes=None)`; `read_consent_token` returns `{pk, scopes}` (legacy bare-int → `{pk, scopes:['overseas_medical']}`, keep compat). `/c` GET → `items[]`; POST `{agreed:[scope]}`; only token-scoped consents recorded (forgery guard); required scope unmet → 412.
- **Consent subject enforcement:** planner-recorded consents are `subject=planner_attested` (server-forced) and CANNOT open the `consent_overseas_at` gate; only `customer_self` (via `/c` or self-diagnosis) opens it. **Registration modal no longer has planner self-check consent boxes** — consent is collected by sending the customer the `/c` link.
- **OCR normalization substring trap** (`core/ocr/ocrparsing.py::COVERAGE_KEYWORDS`): substring + longest-first means a name containing a shorter keyword mis-routes (e.g. '상피내암' contains '암진단'). Fix in dict DATA, NOT the logic. No migration. Add a regression test calling `_match_coverage` directly.
- **Booking timezone:** `availability.py` must keep ALL comparisons in KST aware. WorkHour/recur-block TimeFields are KST wall-clock (combine with the KST date); meetings/single events are UTC (`timezone.localtime`). Mixing them naively shifts 9h.
- **"This month" = KST, not `timezone.now()`** (`dashboard/`): the `__year`/`__month` ORM lookups bucket by `TIME_ZONE`(Asia/Seoul). So any "current month" window MUST use `timezone.localtime()`/`localdate()` — `timezone.now()`(UTC) or `date.today()`(OS-local, e.g. CI=UTC) mis-aligns and drops counts on the UTC/KST month-boundary day. Bit on 2026-07-01 (4 dashboard tests went red at midnight KST). Fixed; apply the same rule to any new monthly aggregation (e.g. `billing` UsageMeter).
- **`NEXT_PUBLIC_API_BASE` build-time inline:** changing it in Vercel needs a redeploy. If prod FE calls localhost, it's unset/not-redeployed.
- **`FRONTEND_BASE_URL` is effectively required** (default `localhost:3000`): unset in prod → email-verify / consent / booking links generate as localhost = broken.
- **New custom domain = 4 env/console updates, ALL env-driven (no code)** (bit 2026-07-01 when `www.inpa.kr` went live → login CORS-blocked, `access-control-allow-origin` absent on preflight while `in-pa.vercel.app` worked): on Render add the new origin to **`CORS_ALLOWED_ORIGINS`** (`prod.py::env.list`, default `[]`) AND **`CSRF_TRUSTED_ORIGINS`**, set **`FRONTEND_BASE_URL`** to the new domain, and add both `https://www.<domain>`+`https://<domain>` (www + apex) since they are distinct origins. Separately, add the domain to the **Google OAuth client's Authorized JavaScript origins** (Google Cloud Console) or the GSI button 403s ("origin not allowed for client ID"). `ALLOWED_HOSTS` = the BE host only (Render auto-injects its own). A 503 right after a merge is usually the Render free-tier redeploy/cold-start, not a real error (check `/healthz/`).
- **402 hidden in beta:** `FREE_TIER_UNLIMITED=True` bypasses all limit checks. BE already returns 402 `{code:'credit_exhausted', kind, limit, used}` for OCR/analysis/compare/promotion. Flipping the flag without an FE notice modal = users see red errors.
- **SQLite vs Postgres seed trap** (memory `sqlite-vs-postgres-seed-trap`): local SQLite ignores varchar length, prod PG rejects. Validate seeds/fixtures against PG too (e.g. `JobRiskCode.name` max 120). Prod `seed_demo` is manual via Render Shell.
- **SEED_VERSION marker trap (2026-07-04):** `seed_normalization`/`seed_jobs` no-op on boot when `SeedMarker` matches the in-code `SEED_VERSION`. Changing seed DATA (STANDARD_TREE, NORMALIZATION_V0, job_risk_codes.json) WITHOUT bumping `SEED_VERSION` means the next deploy silently ships nothing. Bump the constant in the same commit as the data change.
- **`lead_created_at` is the ONLY inbound-lead discriminator (2026-07-04 review blocker):** `lead_source='introduction'`(소개) is planner-selectable in the create/bulk modals, so NEVER treat `lead_source` alone as "came from /d //p". Only the inbound flows set `lead_created_at`; the 6-month retention purge (`cleanup_expired_leads`) requires it non-null plus 4 more AND-guards — keep that shape for any future lead logic.
- **Next 16 nested-layout openGraph trap (2026-07-05):** defining `openGraph` in a nested layout REPLACES the parent's resolved object — the root `app/opengraph-image.jpg` file-convention image silently drops off those routes. Public token layouts therefore set images explicitly via `lib/public-og.ts` (resolved absolute through root `metadataBase`). Use that builder for any new public route; verified by curl on all 5 token routes.
- **Consent doc-version gate (2026-07-04):** the OCR/medical 412 gates require a `customer_self` overseas ConsentLog with `doc_version == CONSENT_TEXTS_VERSION` (`customers/consent_texts.py`). Changing consent WORDING substantively ⇒ bump the version ⇒ every existing customer must re-consent via /c or /d before NEW Claude calls (by design). Never edit consent copy inline in views/FE — the module is the single source.

## 8. Pre-build compliance gates (zero code until resolved)

1. Coverage baseline (core 담보) source + disclaimer definition. **→ RESOLVED (2026-07-02): source = 설계사 직접 입력 only (Inpa provides NO 적정금액 numbers); `apply-preset` disabled(`PRESET_DISABLED` 400, code dormant). Grading stays `PlannerBaseline`-gated(neutral without). Neutral coverage display (incl. the expanded 담보 tree) is allowed pre-gate.**
2. Medical (sensitive) data → Claude API overseas-transfer consent form — **legal prerequisite**.
3. Switch comparison-doc §97 legal requirements finalized.

→ Do NOT start AI-analysis / comparison-doc features before these 3. If blocked, build OCR + coverage table (neutral features) first. Build order (docs/07 §0): common components → OCR + normalization → coverage table → switch comparison → customer message → customer detail / gaps.

## 9. Locked product decisions

- **Auth = email/password + Google OAuth** (KakaoTalk dropped). Signup → email-verify → login → password-reset (email token). PBKDF2 hash; Django signed tokens. Google login links to an existing account by verified email; new Google users collect credential/affiliation in onboarding. Google Calendar auto-creates an event on meeting **accept** (name masked by default). All behind `GOOGLE_OAUTH_*` (unset = hidden).
- **Data visibility:** boards / notices / FAQ / promo samples = **shared**. Everything else (customers, consent, insurance, analysis, compare, calendar, KPI, alerts, baselines) = **owner-only**. 1:1 inquiry = author+admin. Promo orders = owner+admin. `Customer.owner on_delete=CASCADE`.
- **Landing** (`/`, public): hero "설계사님은 클로징만 준비하세요." `AudienceSection` (individual vs manager) + manager taglines.
- **Booking** = planner sets recurring work hours → customer picks a free slot via `/b` link → planner accepts → confirmed (no auto-send; planner copies the link/message).
- **Promotion** = sample photo + Google-form-style input + booking → ops team manual production (no auto-send).
- **Freemium quota:** beta unlimited (`FREE_TIER_UNLIMITED`); numbers/payment post-launch. `PlannerBaseline` presets: beta is direct-input only.
- **Dropped/deferred:** business-card Vision OCR = deferred (manual fallback). Point/cash rewards = improper-benefit risk under §98 → dropped (legal hold). "지점장"→"관리직" wording is UI copy only.

## 10. Working with the PM

- PM is **non-developer**, communicates in **Korean**, reads `README.md` only. Reply in Korean, plain words, no consulting jargon.
- **Plan 90% / Execute 10%:** new features → agree on a roadmap-style plan BEFORE coding; present options as pros/cons with a recommendation. PM often pushes "바로 해줘" — honor momentum, but for **schema/migration or semantic-redefinition changes still surface a 2-line plan + confirm** first.
- Compliance (overseas-transfer consent, improper-switching, ad review) is a feature gate — never bypass.
- **Close every deploy by updating README.md + CLAUDE.md.** Standing rule, do it unprompted.

## 11. Changelog (newest first — condensed; the durable detail lives in the sections above)

- **2026-07-12 (검색·AI 노출 SEO/AEO 토대 + 공개 FAQ — PM 확정, PR #82 DEPLOYED):** FE only, BE·마이그레이션 0, 고객 대면 토큰 라우트 무변경. brainstorm→spec(`docs/superpowers/specs/2026-07-12-search-ai-discoverability-design.md`)→구현→적대 3렌즈 검증(스키마·현행 봇 / 카피 정직성 / 리팩토링·완결성).
  - *robots.ts:* `rules` 배열 재작성 — `*` + **AI 봇 11종**(GPTBot·OAI-SearchBot·ChatGPT-User·ClaudeBot·Claude-User·Claude-SearchBot·anthropic-ai·PerplexityBot·Perplexity-User·Google-Extended·Applebot-Extended) 각각 공통 `ALLOW`(랜딩·legal·data-policy·faq)/`DISALLOW`(토큰 5종·admin·api). 토큰 트레일링 슬래시 유지(§7 접두매칭 트랩).
  - *구조화 데이터:* `components/structured-data.tsx` — `JsonLd`(dangerouslySetInnerHTML + `<` 이스케이프, 정적 상수라 안전) + `ORGANIZATION`/`WEBSITE`/`SOFTWARE_APP`(랜딩) + `faqPage()`(/faq). 조작 신뢰신호(aggregateRating/review/award) 미포함(정직성 레드라인). logo=ImageObject, brand=Brand.
  - */faq:* `app/faq/page.tsx` 서버 컴포넌트 — `FAQ_ITEMS` 10문항 단일 소스 → 화면 렌더 + FAQPage JSON-LD 동시(불일치 방지). canonical `/faq` + 페이지별 OG(전역 `opengraph-image.jpg` 명시 = §7 nested-og 트랩 회피). 정직성: '가입 시 무료 쿠폰 지급' 미보장 문구 제거(쿠폰=어드민 발급만, 베타 무료 사실대로) + 데이터 격리('다른 설계사가 볼 수 있나요') 문항.
  - *랜딩 서버화:* `app/page.tsx` client→server(metadata `alternates.canonical:'/'` = UTM 파라미터 URL 통합 + 서버 JSON-LD), 클라 부수효과(로그인 리다이렉트·UTM)는 `components/landing-client.tsx` 분리(렌더 불변 실증). 푸터 /faq 링크(landing-sections.tsx).
  - *기타:* `public/llms.txt`(llmstxt.org 관례). sitemap `/faq` 추가. layout `verification`(네이버 코드 하드코딩 기본값 `ad315…`, env `NEXT_PUBLIC_NAVER_SITE_VERIFICATION`/`_GOOGLE_SITE_VERIFICATION`로 덮어쓰기; 구글은 도메인 인증 완료라 메타 불요). `public/inpa-logo-120.png`(schema logo, `design/logo/exports/`의 색상 마크 120px). `design/logo/exports/`(로고 5종 PNG/JPG, 미커밋 로컬).
  - *검증:* build + lint:copy(131) + **프로덕션 실서버(www.inpa.kr) 실출력 확인**: naver meta·robots 12블록·sitemap /faq·llms.txt 200·FAQPage 10문항·랜딩 3스키마·canonical·전체 섹션 렌더. 적대 3렌즈 실결함 1(쿠폰 문구) 수정 후 SHIP.
  - **남은 PM 콘솔:** 네이버 서치어드바이저 소유확인→사이트맵 제출, 구글 서치콘솔 사이트맵 재제출. ★ **랜딩 히어로/요금제의 기존 '한 달 무료 쿠폰' 문구(이번 diff 아님)도 미보장**(쿠폰 자동발급 미배선) — 유료 전환 전 문구 정합 or 자동발급 배선 필요. FAQPage는 구글 FAQ 리치결과 폐지(2026-05)로 별점 미표시, Bing·AI 인용엔 유효.
- **2026-07-09 (④ 비교안내서 설계사 직접 발송 + 제안 vs 제안 자유 선택 UI — PM 확정, PR #80 DEPLOYED):** ③④⑤ AI/컴플라이언스 방향의 마지막 조각. **FE only, BE·마이그레이션 0, 고객 대면(/s·/d·/c·/b) 무변경.** 구현 sonnet / 적대 리뷰 opus(§97 중심) / 픽스·재검증 Opus 오케스트레이터(독립 3렌즈 verify).
  - *자유 A/B 선택 UI:* SwitchTab 선택 영역을 두 풀(보유/제안 분리)에서 **단일 목록 + 행별 A안/미포함/B안 3-state 토글**(`AssignInsRow`, `assign: Record<id, "A"|"B"|"none">`)로. 보유·제안 무관 임의 배정 → **제안 vs 제안·증권 vs 증권** 비교. 기본 프리셋 held(pt1)→A·proposed(pt2)→B(회귀 방지). 배정→`sideAIds/sideBIds`→기존 `compareCustomer`(BE 지원 기존).
  - *④ '고객에게 보낼 내용 복사':* 담보 비교표 아래 버튼 → **중립 사실만**(담보별 A안/B안 금액·증감 라벨 + 보험료 요약 + 정직성 1줄) 텍스트를 `copyText`로 클립보드 복사 → **설계사가 직접 카톡·문자 발송**(인파 미발송, §97 회피). **판정·switch_warnings 절대 미포함**. `COMPARE_PUBLISH_ENABLED`(인파 공식 발행)과 무관 = 설계사 복사이지 인파 발행 아님.
  - *적대 리뷰 실결함 수정(CRITICAL·MAJOR):* **(a) 열 이름 사실 정확성** — A측 전부 보유(pt1)·B측 전부 제안(pt2)인 표준 갈아타기 배치일 때만 '현재/제안', 아니면 중립 **'A안/B안'**(`canonicalSides`). 제안 vs 제안에서 A를 '현재'로 오표기해 고객 텍스트가 거짓이 되던 문제 차단(온스크린 라벨·`ComparePremiumSplit` labelA/labelB prop도 동일 적응). **(b) 미포함 재유입** — 미포함을 키 삭제가 아니라 명시 `"none"` 저장 + `loadInsurances` 병합을 `it.id in prev`로 → 새로고침 때 뺀 보험이 프리셋으로 되살아나 고객 텍스트에 섞이던 버그 수정. **(c) 카피가드 사각** — 고객 전송 텍스트 빌더를 **`lib/compare-export.ts`로 분리** + `check-copy.js`에 `lib` ROOTS 추가 + `ADVICE_PATHS`(권유어 규칙)에 그 모듈 포함(설계사 페이지 전체는 정당한 §97 내부 문구 `갈아타기(승환)…`로 오탐이라 파일 단위 분리). **(d)** 양쪽 배정 ≥1일 때만 복사(`canExport`).
  - *불리사항 고지 명시(PM 지시):* 고객 복사 텍스트는 해지손실·면책·이율 변동을 의도적 제외(설계사 내부 전용) → 설계사 화면 2곳('확인해야 할 사항' 박스 + 복사 버튼 아래)에 **갈아타기면 불리사항을 고객에게 따로 안내**하라고 명시. **★ 정책 노트(CPO/법무):** 설계사가 이 복사 텍스트만으로 §97 불리사항 고지를 갈음하면 안 됨(고지 의무는 설계사). `COMPARE_PUBLISH_ENABLED` 법무 검토 전까지 계속 닫힘. **→ 이걸로 PM AI/컴플라이언스 방향(③④⑤) 완료.**
  - Verified: FE `npm run build`(typecheck clean)·`lint:copy`(128파일 0, lib 스캔 추가), 독립 3렌즈 적대 재검증(§97 텍스트·상태 정확성·카피가드) 실결함 0=SHIP. Spec `docs/superpowers/specs/2026-07-09-compare-freepicker-export.md`.
- **2026-07-09 (AI 비교 중립 재정의 ③ + AI 전송 전 신원 마스킹 ⑤ — PM 확정, PR #79 DEPLOYED):** BE **743 tests**, 마이그레이션 0, 고객 대면 무변경.
  - *③ 비교 중립화(§97 위험 감소):* 비교를 '갈아타기 판단'이 아니라 중립 나란히 시각화로, 판단은 설계사. `compute_verdict`(KEEP/SWITCH/NEUTRAL) 응답·호출 제거(함수 보존·미사용 표시), FE 판정 색배지·순편익 제거. `switch_warnings`(해지손실·면책·이율)는 중립 '확인 사항'으로 유지하되 **실제 교체(A측 보유+B측 제안)일 때만** 면책·이율 경고(제안 vs 제안 오발동 수정). **side_a_ids/side_b_ids** 신규: portfolio_type 무관 임의 두 세트(제안 vs 제안·증권 vs 증권), 하위호환(미지정=보유/제안 분리 + current_ids/proposed_ids). FE 라벨 중립화·'부당승환' 헤드라인 순화. **COMPARE_AI_ENABLED/PUBLISH 그대로 닫힘(CPO 판단)**. 누수가드(verdict 부재) 통과. ⚠️ FE 자유 재조합 선택 UI는 후속(BE 지원됨).
  - *⑤ AI 전송 전 신원 마스킹(국외전송 개인정보 최소화):* `core/ocr/pii_mask.py::_strip_identity` — 주민번호·전화·이메일 + 신원 라벨(계약자/피보험자/수익자/성명/가입자) 뒤 한글 2~4자 이름만 마스킹(NOT_NAMES 가드). **담보명·금액·회사명·날짜 절대 미마스킹**(마스킹 후 담보 인식 불변 실증). claude_parse·verify 전송 텍스트에 적용(self_diagnosis 상속). ★ contractor_name/insured_name을 Claude 응답이 아니라 **로컬 정규식(원문 기준)**으로 추출 → 마스킹 후에도 이름 저장 정상 + 더 사적. 재매핑 불필요(고객 이미 앎). **병력 국외이전 동의는 별개 유지(가명처리≠동의 대체, CPO 판단)**. Specs `docs/superpowers/specs/2026-07-09-{compare-reframe-neutral,ai-pii-masking}.md`.
- **2026-07-09 (요금표 한도 정합 A — PM 확정, PR #78 DEPLOYED):** 랜딩 요금표 4한도를 billing이 실제 강제하게 정합 + 신규 고객 추가 한도 신설. ★ 베타 dormant(FREE_TIER_UNLIMITED). `Plan.limit_customer`(billing 0009 additive) + `credit.py` 'customer' kind + `check_and_consume_n`(bulk 전량 402). `CustomerViewSet.create/bulk_create` 강제(검증 후 소모). **셀프진단·소개 인바운드 리드는 미집계**(설계사 능동 추가만). seed_billing: Free 5/1/5/5, Plus·Manager 100/50/50/30, Super 무제한(증권/비교/리포트=보장분석/고객; promotion 불변). **SeedMarker 가드(`seed_billing_quota_align`)로 1회성 baseline**(매 배포 관리자 편집 되돌림 방지, QUOTA_ALIGN_VERSION bump로 재정합). FE 사용량·admin 표시 + 402 kind='customer' 업그레이드 안내. 무료 '최대 5인' → **월 5명 통일**(총량 하드캡 원하면 후속). BE 728 tests. Spec `docs/superpowers/specs/2026-07-09-pricing-limits-align.md`.
- **2026-07-09 (담보 세분류 개별 인식 — PM 확정, PR #77 DEPLOYED):** 소액암·갑상선암(→유사암)·특정암(→일반암)·질병/고도후유장해(→상해후유장해)를 **각각 별도 담보로 인식**. 표준트리 leaf 존재 → §8 무관. **2026-07-02 특수수술과 동형 5-way**(폴백 + 라이브 동시). 라이브: `claude_parser.py::_COVERAGE_CATEGORIES` 프롬프트 외과적 수정(5 세분류 별도 카테고리, 타 섹션 무변경) + `_CATEGORY_MAP` 5키 + `ocrdata.py` dict_detail_data 5 leaf. 폴백/측정: `ocrparsing.py` COVERAGE_KEYWORDS 5 경로 + `coverage_bridge` PARSER_TO_STD 5 + 골든 앵커 8. **골든 0.6569→0.7125, 기준선 0.70**. ★ 기존 파싱 데이터 불변(마이그레이션 0, **새 업로드부터 적용**), 프롬프트 미준수 시 부모로 graceful 폴백. 적대 리뷰(opus) 블로커 0. 라이브 실증 `insurances/tests.py::CoverageSubtypeSplitLiveTests`(mock SDK). **배포 후 실 Claude 스팟체크 권장**(소액암/특정암 든 증권). Spec `docs/superpowers/specs/2026-07-09-coverage-subtype-split.md`.
- **2026-07-09 (사망 담보 정확도 수정 + Manager 요금제 팀 게이트 — PM 지시, PR #75 DEPLOYED):** BE **700 tests**, 마이그레이션 1(billing 0008 additive), 고객 대면 무변경.
  - *사망 세분류 오분류 수정(담보 정확도):* §7 substring 트랩 — 범용 `사망보험금`(5자)이 구체 `재해사망`/`질병사망`(4자)보다 길어 longest-first 로 먼저 매칭 → 재해·질병·상해 사망보험금·보장이 전부 일반사망으로 오분류. `ocrparsing.py::COVERAGE_KEYWORDS` 사망 세분류에 `{재해/질병/상해}사망보험금·보장·특약` 추가(데이터로 고침, 로직·표준트리 불변 = §8 무관). **골든 정확도 0.6176 → 0.6569(+3.9%p, 10건 교정)**, 회귀 앵커 4개(15개), 기준선 0.60→0.65 상향. 회귀테스트 `DeathCoverageRoutingTests`(매처 직접 호출). ★ **반려한 것(중요, 재작업 금지):** (a) 기저 수술·처치 미매칭 = 2026-05-12 PM 정책('진단 금액만 진단비 버킷')으로 **의도적**. (c) 입원일당 = 키워드 매처가 **의도적 None**(prod는 Claude `_CATEGORY_MAP`로 배치, 실손 오염 방지 백스톱, 기존 회귀테스트가 이동 시도를 반려시킴). (b)(e) 소액암/갑상선암/특정암/질병·고도후유장해 세분류 = 상위 카테고리로 접는 설계 vs 세분류 인식 = **PM 도메인 판단 대기**(leaf 존재, 파서 경로 추가만 하면 됨).
  - *Manager 요금제 팀 권한 게이트(기본 비활성):* 팀 대시보드(`/manager/dashboard/`)·초대 링크(`/manager/invite-link/`)를 Manager 요금제 가입자 전용으로. **★ 기본 OFF: `MANAGER_PLAN_GATE_ENABLED` env default False → 베타 현행 동작 완전 보존**(지금 켜면 결제자 0명이라 전원 잠김), 유료 전환 시 env flip(COMPARE_AI_ENABLED 관례, FREE_TIER_UNLIMITED와 독립). `Plan.can_use_team` bool(billing 0008) + `seed_billing`이 manager만 True(재시드 보정, 다른 필드 불변) + `credit.py::user_can_use_team`(활성·미만료 구독). 게이트 ON+무자격 시 402 `manager_plan_required`. `invite-info`(공개)는 무변경. FE: `/manager` 402 → 'Manager 요금제 안내' 카드(상태우선·자기적격 §6) + upgrade-modal manager(19,900 VAT 별도) + PlanAdmin `can_use_team` 노출. Spec `docs/superpowers/specs/2026-07-09-manager-plan-gate.md`.
- **2026-07-08 (활성화 퍼널 계측 + UTM + 인증 멈춤 알람 — 프리런치 #16, Phase 2 로드맵 ⑥/마지막, PR #74 DEPLOYED):** 가입→인증→첫고객→첫분석→첫공유→활성화 코호트 퍼널을 관리자 화면에 노출 + UTM 유입 캡처 + 데드맨 알람. **이것으로 Phase 2 로드맵(#26·#27·#18·#17·#14·#16) 전량 배포 완료.** BE **687 tests**, 마이그레이션 2(accounts 0013 / notifications 0009, additive).
  - *퍼널(온디맨드, 새 이벤트 배선 0):* `admin_console::AdminActivationFunnelView`(GET `/admin/activation-funnel/?days=`, IsAdmin, @inpa.local 제외, days 클램프). 단계 타임스탬프 = 가입 `User.date_joined` · 인증 `Profile.email_verified_at` · 첫고객 `MIN(Customer.created_at)` per owner · 첫분석 `MIN(CustomerInsurance.created_at)` per customer__owner · **첫공유 `MIN(NorthStarEvent.SHARE_CREATED.created_at)` per sender**(★ 가변 `Customer.share_sent_at` 아님 — 재발급이 과거 코호트 활성화를 뒤집던 리뷰 blocker). 활성화 = 첫분석 AND 첫공유가 `ACTIVATION_WINDOW_DAYS`(7) 이내. 단계 **중첩(monotonic)** 으로 전환율 100% 초과 방지.
  - *UTM:* `Profile.utm_source/utm_medium/utm_campaign`(accounts 0013) + `RegisterSerializer` optional 수용·정제(`[A-Za-z0-9._-]`·60자). FE `lib/useUtmCapture.ts`(랜딩 `app/page.tsx` 마운트 시 first-touch sessionStorage 저장, 시각 변화 0 → 가입 제출 시 payload, 저장값 우선·URL 폴백). 관리자 UTM 소스별 활성화율.
  - *데드맨:* `NotifType.SIGNUP_VERIFY_FLATLINE`(notifications 0009 + `ADMIN_NOTIF_TYPES`) + `jobs.py::check_signup_verification_flatline`(최근 창 KST 가입 ≥ `ACTIVATION_FLATLINE_MIN_SIGNUPS`(3) & 인증 0 → 관리자 팬아웃, `_create_once` 하루 1회 멱등, **@inpa.local 제외**). `PRODUCERS` 등록(실패 격리·하트비트). 2026-07-07 SMTP 장애 재발 감지용.
  - *FE:* `app/admin/activation-funnel/page.tsx`(시스템 테마, 퍼널 막대·전환율·UTM 표·평균 활성화 일수, 판정어 0). 고객 대면 무변경(랜딩 캡처 비가시). Spec `docs/superpowers/specs/2026-07-08-activation-funnel.md`.
- **2026-07-08 (로딩 스켈레톤 #14 + Claude 비용 계측 #17 — Phase 2 로드맵 ④⑤, PR #73 DEPLOYED):** 공유 브랜치라 단일 PR로 묶여 머지. gitleaks 오탐(테스트 더미 키 `test-key` 3개가 한 줄 = 엔트로피 규칙 오발동) 때문에 **이력 squash + 더미 키 변수화**(`_DUMMY_KEY`, 키 이름 없는 변수 → 규칙 회피)로 정리 후 배포. gitleaks-action이 repo `.gitleaks.toml` 허용목록을 무시해 config 대신 소스 패턴 제거로 해결.
  - *#14 로딩 스켈레톤:* /s·/b·/c 초기 로딩을 형태 일치 브랜드 스켈레톤으로 통일(2~5초 잔여 대기용). **패널 kill한 30~60초 웨이크 화면·자동 재시도 미구현**(Render Starter 상시가동으로 콜드스타트 해결됨). `components/token-skeleton.tsx`(공유 프리미티브 `SkeletonBar/Card/Row` + `TokenLoadingShell`: accent-tint 헤더 + 작은 `InpaMark live`), /b·/c '불러오는 중…' → 형태 스켈레톤 + '불러오고 있어요', /s ShareSkeleton 리팩토링, /d 무변경(폼 즉시 렌더). 라이트 고정·§6.
  - *#17 Claude 비용 계측:* 호출 1건=1행으로 토큰·추정비용(원)·파싱결과·회사별 미매칭율 기록 → 관리자 화면. `billing.ClaudeApiLog` 확장(user FK·cost_krw·parse_outcome·carrier_code·matched/unmatched, **billing 0007 additive**) + `billing/pricing.py`(모델계열 substring 단가 opus $5/$25·sonnet $3/$15·haiku $1/$5 + 환율 `CLAUDE_USD_KRW_RATE` env 1400, **cost=추정치**, 미상모델→opus 보수 fallback) + `log_claude_usage` 확장(하위호환 kwargs). **4개 호출 경로 전부 성공·실패 기록**: ocr_parse(1회, 실패도 outcome 신호) · **ocr_verify(usage=None 하드코딩 버그 수정→실제 토큰)** · self_diagnosis(공개 /d, user=null) · compare_guide. `claude_parser.claude_parse(meta=None out-param)` 기본 None=동작 무변경, `_set_meta`로 7 outcome(no_key/package_missing/json_invalid/timeout/api_error/success/empty) 채움. 어드민 `GET /admin/claude-cost/?days=`(IsAdmin·@inpa.local 제외·0나눗셈 3곳 가드·user=null 행 유지) + FE 다크 화면(총 추정비용·성공률·일별추이·기능별·회사별 미매칭율, **비용 '추정' 명시**). 잔여 print→logging(compare/credit/events).
  - *PII 레드라인:* 로그·DB 행에 model·outcome enum·토큰수·회사코드 int·건수만(raw 담보명·응답본문·고객식별 0). 전용 회귀테스트 `ClaudeApiLogPIISafetyTests`(필드셋 고정). BE **668 tests**, FE build+lint:copy(115파일 0). 적대 리뷰 2회(opus 과부하 중단 → 오케스트레이터 직접 9영역 + sonnet 재확인) 블로커 0. Spec `docs/superpowers/specs/2026-07-08-{token-page-skeletons,claude-cost-telemetry}.md`.
- **2026-07-08 (골든셋 정규화 정확도 회귀 게이트 — 프리런치 #18, Phase 2 로드맵 ③, PR #72 DEPLOYED):** 정규화 사전(인파 유일 취득자산)의 정확도를 CI 상시 측정·회귀 방지 + #26 승인 게이트 재사용. PM이 #29 유지율 대신 선택(#29 = 보험사 연동 부재로 해약 여부 불가지 → 정직성 문제, 패널 deferred 확정). 구현/리뷰 sonnet·opus / 픽스 Opus 오케스트레이터. BE **638 tests**, 마이그레이션 0.
  - *코퍼스:* `analysis/data/golden_set.json`(회귀 앵커 11: §7 substring 함정 5 + 3대 진단비/후유장해 대표 6) + `NORMALIZATION_V0`(자체 사전) live 로드. `(company,raw_name)` dedup(앵커 우선=하드게이트 승격). **보험사 원본 문서·samples/·benchmark/ 절대 미참조**(패널 provenance 조건).
  - *채점기:* `analysis/golden_eval.py::evaluate_golden_set()` — prod 매칭 순서 재현(admin_verified exact-match → `_match_by_keywords` → `resolve_std_detail`), 순수 함수. `GOLDEN_SET_MIN_ACCURACY=0.60`(실측 baseline 0.6176 바로 아래). `find_golden_expected` 코퍼스 조회(DB 무접근).
  - *CI 게이트:* `GoldenSetGateTests`(정확도 바닥선 + **앵커 100% 하드게이트**). aggregate 는 gross 바닥선, 세밀 회귀는 11 앵커가 담당(단일 항목 회귀는 aggregate 0.4%p 만 움직임 → 고빈도 매핑은 앵커 승격). `manage.py eval_normalization` 리포트 커맨드.
  - *가시성:* 어드민 `GET /admin/normalization/accuracy/` + FE 카드. **★ 카드 라벨 = '키워드 매처 재현율 기준선'**(정직성 §6): 62% 는 제품 정확도가 아니라 키워드 매처만 떼어낸 재현율(실 파이프라인은 Claude+검수 사전+가드로 더 정확)이라고 명시. 낮은 이유 = 세분류 담보의 키워드 재현 한계(버그 아님) + 실제 매처 한계 몇 건(사망 substring 등, 후속 리포트).
  - *#26 연동:* accept 시 골든셋 기대와 다른 매핑이면 비차단 경고. ★ 트랜잭션 밖 + try/except(코퍼스 파일 부재에도 accept 롤백 방지) + 매 승인 전체 재채점(238건) 제거(성능). Spec `docs/superpowers/specs/2026-07-08-golden-set-normalization-eval.md`.
- **2026-07-08 (공유(/s) 스냅샷 보관 — 프리런치 #27, Phase 2 로드맵 ②, PR #71 DEPLOYED):** 고객에게 공유 링크를 보낸 시점의 `/s` 화면을 그대로 박제해 §97 승환 분쟁 증거로 보관. **PM 확정: 보존 180일**(리드 파기와 동일). **설계 결정: 비교(갈아타기) 공유가 §97로 존재하지 않아 → 한눈표 공유(/s)를 발급 시점에 스냅샷**(원 스코프 '비교 스냅샷'의 실현). 구현 sonnet / 리뷰 opus / 픽스 Opus 오케스트레이터. BE **625 tests**.
  - *모델:* `analytics.ShareSnapshot`(append-only, `owner`/`customer` **CASCADE**=고객 삭제 시 PII 동반 파기, `payload` JSONField=`_build_share_payload` 결과 그대로 복제=표준트리 FK 0=불변, `consent_overseas`/`consent_doc_version`/`consent_scopes`/`dict_version`(SeedMarker live)/`insurance_count`/`captured_at`/`retention_expires_at` db_index). 마이그레이션 analytics 0002(additive).
  - *캡처:* `CustomerShareCreateView.post` 토큰 회전 후 `_build_share_payload(customer)` 재사용해 create. **try/except + `transaction.atomic()` savepoint 격리** → 캡처 실패해도 링크 201 정상 발급, 예외는 타입명만 로그(`logger.error` exc_info 없음, §7 PII 로그 레드라인).
  - *파기 3경로:* ① TTL — `notifications/jobs.py::cleanup_expired_share_snapshots(now)` + `run_daily_jobs` 단계(08:00 KST). **0 이하 = 파기 중단 안전 스위치**(cleanup_expired_leads 동형; 캡처 stamp도 0 이하 시 180 폴백). ② `public_consent.py::_apply_revocations`가 `personal_info` 철회 시 그 고객 스냅샷 전량 삭제. ③ 고객 삭제 = FK CASCADE.
  - *조회:* owner-scoped `GET customers/<id>/share-snapshots/`(목록 경량, payload 제외) + `.../<snap_id>/`(상세 payload, 소유 격리 404). FE `components/share-snapshot-panel.tsx`(고객 상세 '공유 기록' 패널, 그 시점 화면 읽기전용 재구성, light 고정).
  - *컴플라이언스:* 스냅샷=/s 미러 → 추가 PII 0(이름 마스킹·birth_year·전화/메모/병력 없음·verdict/switch_warnings 미포함, 회귀 고정). 유한 보존+3경로 파기 = '증권 원본 미보관' PIPA 자산 유지(Seo/Dr. Im 조건). privacy 처리방침 보관 고지 1줄. 고객 대면(/s·/d·/c·/b) 무변경.
  - *리뷰 반영:* 보존 0 이하 안전 스위치가 문서와 반대로 즉시 삭제하던 결함(파기 가드+stamp 폴백) 수정, 캡처 로그 exc_info 제거, savepoint 격리. Spec `docs/superpowers/specs/2026-07-08-share-snapshot-archive.md`.
- **2026-07-08 (담보 사전 피드백 루프 — 프리런치 #26, Phase 2 로드맵 ①, PR #70 DEPLOYED):** 설계사가 보장 한눈표에서 잘못 잡힌 담보를 알리면 → 어드민 검수 → `NormalizationDict(admin_verified)` 반영 → 다음 분석부터 자동 교정(데이터 복리). 구현/리뷰/픽스 파이프라인은 Fable 5 서브에이전트(구현 중단→Opus 오케스트레이터가 리뷰 픽스 검증·잔여 테스트 3건 보강). BE **605 tests**.
  - *BE:* `analysis/models.py::CoverageFlag`(owner/customer SET_NULL/analysis_detail SET_NULL/case FK/raw_name_snapshot/company/note/status open·accepted·rejected/resolved_by/memo, status index) + `analysis/flags.py` 설계사 API(`GET customers/<id>/coverage-cases/?detail_id=` 소유격리 + `POST customers/<id>/coverage-flags/` **owner_only=True**(어드민 write 우회 차단) + `_notify_admins` fan-out). 신규 `NotifType.COVERAGE_FLAG_REQUESTED` → `ADMIN_NOTIF_TYPES` 등록(어드민 벨 배지 자동).
  - *어드민:* `admin_console` `AdminCoverageFlagListView`(?status=, 기본 open, page20) · `AdminCoverageFlagResolveView`(accept = 사전 upsert(admin_verified) + `InsuranceDetail.analysis_detail.set([std])` 전역 정정 + 부분문자열 충돌 경고(차단 없음) + 120자 절단 경고 / reject = status+memo, 409 재-resolve) · `AdminNormalizationLeavesView`([표준] 스코프 leaf 피커). 대시보드 `open_flags`.
  - *원문 저장:* `CustomerInsuranceDetail.raw_name`(200) + `CustomerInsurance.company`(SmallInt null) — `_persist_ocr`이 저장, **생명 = 200+index 오프셋**(claude_parser normalizer 호출·UnmatchedLog 적재·플래그 스냅샷 3면 정합; seed_normalization 코드 공간 규약과 일치). 직접입력 경로는 빈 값.
  - *어드민 정규화 매핑 버그 수정:* 기존 FE `adminMapNormalization`이 `{unmatched_id, standard_name}`(잘못) → BE 규격 `{unmatched_log_id, std_detail_id, confidence}`으로 정정(실제로 등록이 안 되던 상태였음) + 타입 필드 `company/occurrence/std_detail_name` 정합 + leaf 피커 도입.
  - *FE:* `components/heatmap.tsx` HeatCell 조용한 깃발 어포던스(데스크탑 hover/focus, 터치 상시; aria "담보 위치가 이상해요") + `components/coverage-flag-modal.tsx`(케이스 선택·메모 300자·긍정 완료 카피). `/admin/normalization` '이상 신고' 탭(원문·회사·메모·설계사·현재 매핑 → 승인/반려 + relink 수·경고 표시) + `app/admin` 대기 카드 `?tab=flags` 딥링크.
  - *리뷰 반영(적대 리뷰 실결함 3):* ① 생명 코드 200+오프셋 일관화(엉뚱한 손보 사전 오염·seed 행 덮어쓰기 차단, 블로커) ② `company<0`(미감지) 승인 시 죽은 사전 행 생성 생략(연결 정정만) ③ `_get_or_create_detail`의 브리지 `.add()`를 **링크 0개 행 한정**(어드민 정정이 다음 업로드에 안 되돌려지고 한눈표 이중 계산 차단). 마이그레이션 3(insurances 0005 / analysis 0003 / notifications 0008, 전부 additive). 고객 대면(/s·/d·/c·/b) 무변경. Spec `docs/superpowers/specs/2026-07-08-coverage-dict-feedback-loop.md`.
- **2026-07-07 (최후순위 묶음 완결 — 법정 표시·가격·결제 데스크·Sentry, PR PENDING):**
  - *법정 표시(H-6 완결):* 약관·개인정보처리방침·랜딩 푸터에 (주)서울엘엔에스금융컨설팅(브랜드 핀고)·대표 황희철·사업자 109-86-17632·통신판매업 2021-서울구로-1990·주소·이메일, 시행일 2026-07-07, **CPO 황예진**. 플레이스홀더('예비창업'·'확정 후 기재'·legal.tsx 초안 배너) 전면 제거.
  - *가격·Super(billing 0005):* Plus **19,900원(VAT 별도)**(placeholder 29,000 조건부 데이터 마이그레이션, 관리자 수정값 보존) + **Super 신설 39,900원(VAT 별도), 한도 무제한(null)**. `seed_billing` super 포함. 랜딩 요금표 3열.
  - *계좌이체 결제 데스크(#12):* UpgradeModal — **VAT 표시 규칙(PM): 노출은 VAT 별도만, 결제 단계에서만 요금제 토글 → 월 이용료/VAT(10%)/최종 입금액(21,890/43,890) 분해** + 국민은행 459001-04-503030(예금주 핀고) 복사 + 절차 3단계(세금계산서 안내). 수동 데스크(관리자 구독 부여), KICC PG 후속.
  - *Sentry(LB#11 완결):* BE = Render `SENTRY_DSN` env(기존 prod.py 코드) · FE = `@sentry/nextjs` 10.x, Next16 컨벤션(instrumentation/-client.ts + server/edge config + withSentryConfig org fingo-dm). **PII 원칙: sendDefaultPii=false·리플레이 0·트레이싱 0·프로덕션만**. DSN 내장(공개 키)+env 재정의.
  - 테스트 573(568→573) · 마이그레이션 billing 0005 1건 · FE build+lint 통과.

- **2026-07-08 (가입 흐름 마감 — PR #66·#67 DEPLOYED):** *오류 실사유 표시:* `lib/api.ts::extractErrorDetail()` — DRF 필드 오류(dict/array)를 6개 정규화 지점에서 통일 추출, 가입 400이 '오류 발생' 한 줄로 뭉개지던 문제 해소. *인증 메일 재발송 버튼:* `/login` EMAIL_NOT_VERIFIED 배너에 '인증 메일 다시 받기'(기존 `resendVerification` 배선; 성공/실패 동일 안내 = 계정 존재 비노출, 재제출 시 showResend 리셋 = §12 이관건 해소). *Sentry:* deprecated `disableLogger` 제거. **PM 가입 실패 실원인 = new.inpa.kr 오리진 잔존 화면에서 가입 시도**(BE CORS·GSI 오리진 차단; www에서는 정상 — §7 도메인 gotcha 실사례, proxy 배포 후에는 서비스 경로가 www로 307이라 재발 없음). Vercel 중복 프로젝트 'extraordinary-transformation'(같은 repo, 빌드 실패 반복) 발견 → PM 확인/삭제 대기.
- **2026-07-07~08 (new.inpa.kr 시네마 랜딩 — DEPLOYED 2026-07-08):** ⚠️ 공유 브랜치 특성으로 **PR #66에 편승해 라이브**(리뷰픽스 8562797은 PR #67로 후속); new.inpa.kr 200 + manager 플랜 프로드 `/billing/plans/` 노출 확인. 구현 완료 커밋 7건: BE `072d873`(Manager plan: models PLAN_CODE+`manager`, migration **billing 0006**(AlterField only), `seed_billing` manager 블록(19900, Plus 동일 한도 200/100/200/100, VAT 별도+관리자 설명), admin 구독부여 choices, 테스트 574 green) · FE `2377380`(www 섹션 → **`components/landing-sections.tsx`** 추출, 정규화 diff SAME = 렌더 불변 실증) · `59d0809`(**`lib/typewriter-sound.ts`** WebAudio 합성 타자음 + **`components/typewriter.tsx`** 글자 타이핑 + globals.css `.tw-cursor`) · `72e4249`(**`components/brand-story-sections.tsx`** 6종: 브랜드정의·여정·프로세스맵(HTML 재구성)·클로징히어로·페르소나(InpaMark **`dotColor`** prop 신설)·**요금 4단** + `public/landing-new/` webp 4장(시안 PDF에서 추출, 28~59KB)) · `433318c`(**`components/cinema-landing.tsx`** 게이트→장면6→스크롤 상태머신 + `app/new/page.tsx`, canonical `https://new.inpa.kr/`) · `081486d`(**루트 `proxy.ts`**: new.inpa.kr host만 `/`→`/new` rewrite·`/new`→`/` 정규화·그 외 경로는 `https://www.inpa.kr`로 307 → **BE CORS/env 변경 불필요**; matcher가 `_next`·확장자 파일 통과) · `8562797`(리뷰 fix 2건: film 키보드 핸들러가 버튼 포커스 시 양보(접근성) + 공백 글자 타자음 복원). 검증: BE 574 tests·check·makemigrations --check clean, FE build(`ƒ Proxy` 표시)+lint:copy 111파일 0, host-헤더 curl 스모크(rewrite/307/www 불변) 실출력 확인. 원 설계 요약(하이브리드·게이트·장면 구성·요금 4단 VAT 병기)은 spec = SSOT. PM 시안 `landing_page.pdf`(14p, untracked) 기반 브랜드 스토리 랜딩을 새 도메인 **new.inpa.kr**에 신설 확정 (www `/` 불변). **Spec = `docs/superpowers/specs/2026-07-07-new-inpa-cinematic-landing-design.md`** (SSOT; 여기는 요약). Hybrid: click-through cinema (시안 p2~p8: 人波 정의 → 흩어진 업무 2비트 → INPA:Insure Partner 반전 → 군중 → 파랑 약속; 글자 단위 타자기 타이핑 + WebAudio 합성 타자음, 입장 게이트 '소리와 함께 시작/조용히 보기' = autoplay 정책 대응, 건너뛰기·음소거 상시, reduced-motion = 즉시 표시) → 밝아지며 scroll landing (시안 p9~p14 + **www `app/page.tsx` 섹션 재사용**: TrustBar·핵심기능·기능한눈에·차별점·어떤분에게·어떻게쓰나요·원칙·CTA·푸터 → 공용 추출 리팩토링, www 렌더 불변 조건). 시안 p1(발표용 포스터)은 제외, p2부터 시작(PM). 요금제 = 시안 4단 공개(Basic 0 / **Manager 19,900 — billing 미존재 플랜, 유료 전환 전 등록 후속** / Plus 19,900 / Super 39,900) + `(VAT 별도)` 병기(전사 규칙, 시안과 유일한 의도적 차이) + 쿠폰 부제·가입자 FREE 블록. 기술: `app/new/` 라우트(작업명) + **Next 16 `proxy.ts`**(middleware 아님) host-rewrite; new는 www와 달리 로그인 리다이렉트 없음. 시네마 파트의 검은 화면은 영화 연출이며 다크 모드 아님(§6 테마 가드레일과 별개; 스크롤 파트는 라이트).
- **2026-07-06~07 (PM 스모크 후속 — PR #61·#62·#63 DEPLOYED):**
  - *오늘 전화 독립 메뉴 (PR #61):* 홈 카드 제거 → `/call-list` 전용 화면(공용 `components/call-list.tsx`, limit=50) + 사이드바 '오늘 전화'(고객 다음) + 모바일 더보기 첫 항목. BE call-list `?limit=`(기본 10, 상한 50).
  - *가입 500 긴급 수정 (PR #62):* Resend SMTP 실패가 유저 생성 후 인증메일 발송에서 500으로 번짐(실서버 재현) → `accounts/views.py::_try_send()` 발송 격리(가입 201 + `email_sent` + 재발송 안내; resend/reset은 200 유지 = 계정 존재 노출 방지). **운영 픽스: Resend 도메인 inpa.kr 인증 + `DEFAULT_FROM_EMAIL` 오타(.com→.kr) 교정 → `email_sent:true` 실서버 확인.** 설계사 번호 검증 완화(숫자14 → 영문/숫자/하이픈 4~20, FE 자릿수 노출 제거), 약관 체크박스 중앙 정렬, 알림 페이지 폭 1440px.
  - */d 전용 OG (PR #62):* `public-og.ts` 라우트별 이미지 인자 + PM 제작 `public/og-self-diagnosis.jpeg` 연결(메인과 분리).
  - *셀프진단 다중 증권 (PR #63):* `files[]` 최대 5장(단일 `file` 하위호환), 파일별 파싱 격리 + **장당 일일 캡 소모**(비용 가드), 리드 1명 귀속, 응답 `insurances[]`(보험별 pruned tree/status/message). FE 다중 선택·파일별 제거, 2장 이상 = 보험 카드 목록 → 탭 시 개별 보장 상세(한 번에 하나). `/s`·`/d` 트리 빌더 공용화(`build_coverage_tree`, /s 출력 불변). 테스트 546→554.
  - *백업 실설치(PM 서버):* 우분투 20.04 = PGDG 종료 + Neon **PG18** → 도커 우회(sed 패치, `docs/dev/26` E-2절) 실적용, 첫 암호화 백업 성공. 메일 정상화와 함께 감사 H-8/H-3 실운영 완결.
  - */analysis 보험 카드 뷰 (PR #64):* 보유 2건+ = '전체' + 보험별 카드 → 클릭 시 그 보험만의 한눈표(`?insurance_id=` 필터, 소유 격리 404 — 크레딧 소모 전 차단, 전체 복귀는 재조회 없음). 유료 전환 메모: 필터 조회도 분석 크레딧 1회.
  - *판촉물 주문 개선 + `Profile.phone` (PR #64, accounts 0012):* 회신 이메일(`_reply_email`, 계정 프리필+이중 검증)·추가 요청(`_extra_request`)·'인쇄' 라벨 필드 프로필 자동 채움 + 완료 카드 신설. **전화번호 저장 → `/s` planner_contact 자동 활성(전화/문자 버튼)**, 마이페이지 전화번호 카드. 테스트 559→568.
- **2026-07-04~05 (MVP 스프린트 — 리뷰 티어 'MVP' 5건, 3 commits, PR PENDING):**
  - *오늘 전화할 고객 (#19, 채점 1위):* `customers/views.py` call-list @action(생일 D≤7 > 만기 D≤30 > 무접촉≤60점 + TA/FA +10, 사유 0 제외, top10+total) + 홈 좌측 파이프라인 아래 카드(사유 칩 + tel/sms/화법 원탭, 화법 프리필=이름 기반 기존 리더). KST 앵커, 날짜 파싱은 notifications.jobs 헬퍼 재사용.
  - *카톡/OG 미리보기 + SEO (#20·22):* 공개 5종 라우트별 정적 OG(`lib/public-og.ts` 공용 빌더 — §7 Next16 함정 참조), /p noindex 보강, `app/robots.ts`(토큰 경로 트레일링 슬래시로 /schedule 오차단 방지)+`app/sitemap.ts`, in-pa.vercel.app 잔재 0.
  - *화면 상태 정리 (#21):* 홈 무음 실패→앰버 배너 1개+다시 시도(reloadKey 전 로더 재실행), 히트맵 '보험 있는데 담보 0' 중립 안내, 401 처리 `handleUnauthorized()` 통일(수제 fetch 4곳), 바텀내비 시트 배지 렌더.
  - *권유 단어 차단 (#23):* check-copy.js 경로 한정(s/b/c/d/p) 규칙 6종(추천(?!인) 등, 추천인·내부화면 오탐 0 실증) + `core/copyguard.py` 서버측 관측 가드(공유/자가진단 고정 카피).
  - *팀 초대 링크 (#24):* `accounts/invite.py`(TimestampSigner, `TEAM_INVITE_TTL_DAYS` 기본 7, 응답에 ttl_days) + `POST /manager/invite-link/` + 공개 `GET /manager/invite-info/`(throttle invite_info) + RegisterSerializer `invite_token`(manager FK+빈 소속 프리셋만, **share level 절대 미변경** 회귀테스트, 무효 토큰≠가입 차단). FE /manager 카드 + /register 초대 칩.
  - Verified: BE **546 tests**(524→546), FE build + lint:copy(권유 단어 규칙 활성 상태 104파일 0). 마이그레이션 0. 잔여 MVP: #15 유료 파일럿·#25 지점 코드(최후순위 결제 데스크에 종속), #16 활성화 계측·잔여 Backlog은 후속.
- **2026-07-03~04 (프리런치 리뷰 + 출시 차단 스프린트 — 9 commits on `feat/design-refactor`, PR #59 PENDING MERGE):**
  - *리뷰(2026-07-03):* 코드 전수 감사(6영역 병렬+검증) → 22-persona 3라운드 토론(제안 88건→33) → 정량 채점(660표) → `docs/prelaunch-review/` 5종(project_audit·debate·features·scores.csv·summary_ko). LB 13종 실행 순서 합의, 법적 거부권 0건. **카피 철학(우월감·적합성, §6 신설)** 12개 문구 반영(랜딩 '월 N건' 제거 포함).
  - *동의 v2 (LB-2):* `customers/consent_texts.py` 단일 소스(`v2-2026-07-04`) + 공개 `GET /consent-texts/` + 전 ConsentLog 생성경로 `doc_version` 스탬프. OCR·병력 게이트가 '현재 버전 customer_self 동의' 요구(412 `reason=missing|reconsent`), 구버전 동의는 /c·/d 재동의로 회복(리뷰 blocker: /c `_already` 버전 인지 픽스). '즉시 삭제' 허위 보유기간 문구 전면 제거.
  - *시드 안전화 (LB-1):* delete-recreate → 자연키 upsert(PK 보존) + `SeedMarker`(analysis 0002) 부팅 가드 + `repair_analysis_links` 복구 명령. 재시드 2회 후 링크·검수행·집계 불변 실증.
  - *알림 실발송 (H-1):* `notifications/jobs.py` 생산자 5종(생일 D-7·만기 D-30·상담 내일·할일 days_before·미열람 공유) + `POST /jobs/run-daily/`(X-JOB-TOKEN) + GHA cron 08:00 KST + `SeedMarker('daily_jobs')` 하트비트. KST 일 멱등(재실행 중복 0).
  - *백업 (H-3):* `scripts/backup/`(pg_dump→GPG AES256→30일 보존·복구 스크립트) + `docs/dev/26` PM 런북(자체 서버 cron, 분기 복구 리허설).
  - *신뢰 대청소 (LB-3·§6c):* `cleanup_demo`(데모 계정·[DEMO] 플랜·공유 [DEMO] 행, 관리자 보호) + '준비 중' 렌더 문구 0(check-copy.js 규칙 추가, 카카오·네이버 행 삭제) + 판촉물 브랜드 플레이스홀더(빈 이미지·404).
  - *PII 로그 (H-6 코드부):* claude_parser·insurances·verify print → 내용 없는 logging(응답 길이·예외 타입만) + LOGGING 설정 + 레드액션 회귀테스트.
  - *동의 철회 + 리드 파기 (H-7):* /c `revocable`+`revoked[]`(전 로그 도장, 재동의 왕복), `cleanup_expired_leads`(`LEAD_RETENTION_DAYS`=180, PM 확정 6개월; 5중 AND 가드, 리뷰 blocker '소개' 직접등록 오삭제 차단 = `lead_created_at` 판별자) + privacy 고지 1줄.
  - */s 연락 레이어 (LB-8):* booking_url 없으면 tel:/sms:/'연락 요청 남기기'(SELF_DIAGNOSIS_LEAD 재사용, 공유·일 dedupe). planner 전화 실필드 부재 → 현재 콜백만(필드 생기면 자동 확장).
  - *데이터 (LB-13 1단계):* 루트 `data/` 메리츠 3파일 untrack + `/data/` gitignore. 이력 재작성은 출시 후.
  - Verified: BE **455→524 tests**, FE build + lint:copy(em-dash+준비 중), 적대적 리뷰 5회(blocker 2·major 1 실결함 수정). 결제: 계좌이체→KICC PG 확정(사업자 보유), 자체 서버 백업. 잔여 = PM 콘솔 4건 + 최후순위 3건(§1).
- **2026-07-02 (post-feature bundle — 유료화 스위치 + 기준선 방침 + 담보 확장 + 마일스톤, 8 commits on `feat/design-refactor`, subagent-driven, PR #57 DEPLOYED):**
  - *유료화 관리자 토글 (billing, migration 0004):* `RuntimeConfig` singleton(pk=1, `free_tier_unlimited`). `credit.py::free_tier_unlimited()`가 DB값 우선(solo()가 최초 get_or_create 시 env 시드 → 기존 `@override_settings` 테스트 보존; 예외 폴백 = env 기본 **True** = fail-open 베타안전). `GET/PATCH /admin/billing/mode/`(IsAdmin) + Django admin + FE `/admin/settings` '유료화 모드' 토글(확인창). PM이 **재배포 없이** 유료 한도(402) ON/OFF. commits 0cf1333 + 4fd847a.
  - *기준선 = 설계사 직접 입력만 (§8 게이트 #1 해소):* apply-preset(인파 제공 '미검증 권장금액') **비활성**(`customers/views.py::apply_preset` → 400 `PRESET_DISABLED`, 코드 dormant 보존) + FE 프리셋 UI 제거. 인파는 적정 금액 숫자를 제공하지 않음(무등록중개 회피). 히트맵 neutral(기준 미설정) 시 `components/baseline-required-modal.tsx`(비차단, `/settings/baseline` 링크)로 '기준 먼저 설정' 안내. commit ce6df71.
  - *종합보험 미매칭 담보 확장 (정규화 core, NEUTRAL only):* STANDARD_TREE +15 leaf(특수수술8·특수입원5·표적항암2), NORMALIZATION_V0 +47 alias, coverage_bridge PARSER_TO_STD +15, claude_parser `_CATEGORY_MAP`/프롬프트, ocrdata dict, COVERAGE_KEYWORDS +15 — **5-way 정합**. 함정 회피: 수술 키워드 ≠ `진단비->` path(treatment 차단), 입원 ≠ `실손->` path(fixed-benefit 차단), **질병중환자실입원일당 ≠ 일반 중환자실입원일당**(경로 분리, 오흡수 픽스 55f2434). NEUTRAL 표시만(판정은 PlannerBaseline). `chart_based_amount`=차트참조(적정금액 아님). 마이그레이션 0(seed/data). commits 2c47c64 + 55f2434. 회귀테스트 8.
  - *마일스톤 페이지(`/admin/milestones`):* P3c·P4c·P5c·P8c·P9c → 완료(✅), P6c → 전처리 일부 정정. commit eb67c77.
  - Verified: BE **455 tests**, FE `npm run build` + `lint:copy`(100 files, 0), coverage-expansion opus 전체리뷰(교차파일 정합 15/15, 함정 회피 확인).
- **2026-07-02 (갱신/비갱신 요금 분리 — 11 commits on `feat/design-refactor`, subagent-driven SDD):** 보장분석·비교분석·보험카드에서 보험료를 갱신형/비갱신형/적립으로 **표·절대수치**로 분리 표시(도넛·막대·상대수치 없음). foliio 데이터 완결성은 살리되 foliio의 자체기준 판정(레이더/충분도)은 **이식 안 함**. **마이그레이션 0** — 데이터는 pdfplumber 저장 경로에서 이미 계산·저장됨(`CustomerInsuranceDetail.premium`·`payment_period_type`(3=갱신/1·2=비갱신)·`.calculate()` 총갱신/비갱신, 보험단위 `CustomerInsurance.monthly_renewal_premium` 등).
  - *BE:* `insurances/serializers.py` **`CaseFeeSerializer`**(담보별: detail_name·premium·payment_period_type·is_renewal·assurance_amount·총갱신/비갱신) + **`InsuranceFeeSerializer`**(보험별 요약 + `case_fees[]`; 수기입력 보험은 case_list 없어 `[]`); `CustomerInsuranceManualSerializer`에 갱신/비갱신 월보험료 노출. 히트맵 응답에 `insurances: InsuranceFee[]` 추가(기존 `case_list__detail` prefetch 재사용 → N+1 0; `summary`는 이미 월/총 갱신·비갱신·적립 6필드 포함). `compare.py::_aggregate_side`가 6분리필드 None-safe 합산(**한 측 전부 null이면 0 아닌 `None`=미상**, 2-튜플 arity 유지) + `current/proposed`에 `insurances` 첨부. verdict(KEEP/SWITCH)·rows·`/s` 공유뷰 **무변경**(고객 대면 미노출 유지).
  - *FE:* `lib/api.ts` 타입(`InsuranceCaseFee`·`InsuranceFee` + `HeatmapSummary`/`CompareSide`/`ManualInsuranceItem` 확장); **`components/premium-split.tsx`** 신규 — `PremiumSplitSection`(월/총 요약표 + 담보별 요금표 + 중립 '데이터 확인' 안내: gap=월−갱신−비갱신−적립, overage/unclassified) + `ComparePremiumSplit`(현재↔제안 증감표, 증감=**절대금액+부호**). 고객상세 분석/비교 탭 + `InsuranceCard` 배선.
  - *컴플라이언스(레드라인):* 보험료엔 판정어·등급·색 **0**(사실 숫자만). 등급은 보장금액에만·설계사 기준(PlannerBaseline)일 때만. 추출은 **pdfplumber**(전자 PDF 텍스트, 이미지 PDF 거부) — 실제 OCR 라이브러리 없음(코드의 'OCR'·`core/ocr/`는 레거시 오칭). 전체 리뷰(opus) Ready-to-merge=YES · BE 445 tests · FE build · lint:copy 0. 스펙/플랜: `docs/superpowers/{specs,plans}/2026-07-02-renewal-premium-split*`.
- **2026-07-02 (cleanup + 1-month coupon bundle — 7 commits on `feat/design-refactor`, PR #55, DEPLOYED):** backlog cleanup + the paid-launch coupon. Verified: full BE suite 438 tests green, FE `npm run build` green.
  - *noindex (fix):* `SelfDiagnosisView`(/d) + `IntroductionCardView`(/p) now inherit `_NoIndexMixin` (X-Robots-Tag) — the last 2 of 5 public token views were missing it (throttle/ref-gating already present).
  - *.env.example:* DB URL example MariaDB → PostgreSQL(Neon).
  - *GapTab removed:* dead `GapTab` fn + `"gap"` TabKey + unused `HeatmapDetail` import purged from `customer/[id]/page.tsx`.
  - *OCR manual fallback (P9c):* `OcrStatusBanner` gained optional `onManualEntry` → "직접 입력으로 등록하기" button on OCR failure (3 call sites: 고객상세 분석/비교, /analysis; positive-framing).
  - *Legacy MeetingSlot retired:* removed `MeetingSlotViewSet` + `/meeting-slots/` route + `MeetingSlotSerializer` + FE `listMeetingSlots/createMeetingSlot/deleteMeetingSlot` + `MeetingSlot` type + AvailabilityShare '등록한 시간 자동' 탭(orphaned → **manual-only** now). **Model + `Meeting.slot` FK + admin kept** (no destructive migration). 4 dead tests removed.
  - *OCR normalization (safe, §8-respecting):* `_detect_company` **Phase 4** — company-unknown **변액·종신·유니버셜** products classified `'unknown'→'life'` (company stays `-1`, no guess → premiums/dates/coverages then parse via the life path). `_match_coverage` preprocessing gained `(1차)/(제2차)` round-marker strip. **No standard-tree expansion** (§8-gated: 종합 미매칭 담보 still deferred). 2 regression tests.
  - *Copy-guard lint (P8c):* `inpa_fe/scripts/check-copy.js` blocks em-dash(—) in rendered `.ts/.tsx` strings (comments exempt via strip heuristic); `npm run lint:copy` + **CI frontend gate** (before build). Word rules (안전/심의완료/추천) intentionally excluded (false-positives on disclaimers/rule-text). Extensible `RULES` array.
  - *1-month free coupon (billing, migration 0003):* **admin-issued codes** (PM decision). `Coupon` (auto-gen `INPA-XXXXXXXX`; plan/duration_days/max_redemptions/expires_at/is_active/note) + `CouponRedemption` (`(coupon,user)` unique). `POST /billing/coupons/redeem/` grants the coupon's plan (Plus) for `duration_days` (subscription upsert; **same-plan stacks**; 404 not_found / 409 already / 410 expired·exhausted·inactive; `select_for_update`). **★ `credit.py` now expiry-aware:** a subscription past `expires_at` falls back to Free limits (null `expires_at` = 무기한 → existing manual-Plus unaffected). Issuance via Django `CouponAdmin` (non-dev accessible). FE: `settings/account` '무료 쿠폰' card + `redeemCoupon`. 8 tests. **Closes the C12 coupon-entitlement prerequisite for `FREE_TIER_UNLIMITED=False`.**
- **2026-07-01 (session UX/feature bundle — PR #52, DEPLOYED):** one PR bundling this session's fixes+features.
  - *Auth 401 handling* (`lib/api.ts`): an authed request returning **401** now clears the token + redirects to `/login?session=expired` (guarded: skips if already on `/login`, JSON login/register 401 still surfaces as error). Root cause of the "유령 401 + 화면 안 넘어감": FE swallowed post-load 401s and never logged the user out. `/login` shows a neutral session-expired banner. `uploadProfileImage` mirrors the same 401 handling.
  - *`/schedule` layout* (`app/schedule/page.tsx`): '예약(가용시간) 관리' promoted from a header link → **horizontal card** above the pair; **BookingSettings ↔ AvailabilityShare left/right swapped** (공유문구 now left).
  - *Bulk register fields* (`customer-bulk-modal.tsx` + `customers/views.py::bulk_create`): paste → **editable table** carrying gender·birth_day·job_code(per-row search)·lead_source·memo·avatar(label+color). BE accepts the full set (model-constraint validation, `job_code` only if the id exists, `lead_source` fallback to direct). Migration 0. Test `test_bulk_create_full_fields`.
  - *Signup* (`register/page.tsx` + `RegisterSerializer`): added **소속(affiliation)·직책(title)·설계사 번호(license_no, digit-only 14-length validation)** + a **전체 동의** button (self-diagnosis pattern). Reuses existing Profile fields → migration 0. Tests `test_register_with_planner_fields`, `test_register_rejects_bad_license_no`.
  - *Heatmap grade gating* (`heatmap.tsx`): when `mode!=='graded'`, clicking 넉넉/적정/부족 no longer filters (empty) but shows an inline "보장 기준을 먼저 설정해 주세요" notice + `/settings/baseline` link (positive framing, no negative). FilterChip gained optional `title`.
  - *Real 공지·FAQ* (`boards/management/commands/seed_boards.py`): 실서비스 공지 4 + FAQ 6, no `[DEMO]`, 출시일 비공개·긍정 톤; **idempotent `get_or_create`** keyed on title/question (admin edits survive redeploy); author = first admin; added to `render.yaml` startCommand. Promotion was already real (7 `seed_promotion` skeletons; `[DEMO]` samples only in manual `seed_demo`, not deployed).
  - *Planner profile photo* (`Profile.profile_image` ImageField, migration **0011**): uploaded in **마이페이지(설정 > 계정)**, NOT signup (PM decision). Stored via the existing S3-compatible default storage = **Cloudflare R2** (same bucket as `Customer.business_card`; PM confirmed R2 already wired for 명함 → profile photos persist too). `ProfileSerializer` exposes `profile_image`; `ProfileView` GET/PATCH pass `context={'request'}` for absolute URLs; multipart PATCH via `uploadProfileImage`. Pillow already a dep.
  - *Legal to near-launch* (`legal/terms`, `legal/privacy`): company info = **핀고(Fingo) · 예비창업** (상호·운영단계·CPO placeholder to be filled on business registration); 수탁자 list adds **Cloudflare R2** (image storage), **Resend** (email, TBD removed), **Vercel** (+방문자 통계 for the new Vercel Analytics); **Anthropic retention wording corrected** from "즉시 삭제/응답 후 별도 저장 없음" → "Anthropic 정책에 따름 · 학습 미사용" (the old claim overstated; zero-retention needs a separate agreement).
  - *Vercel Analytics + Speed Insights* (`app/layout.tsx`): `@vercel/analytics` + `@vercel/speed-insights` added. **Must Enable in Vercel dashboard** to collect.
- **2026-07-01 (seed_billing — free-plan + subscription normalization, PR #50, DEPLOYED):** Root cause: `billing/signals.py` (User post_save) auto-creates a free `Subscription` on signup but only when `Plan(code='free')` exists; otherwise it logs a warning and SKIPS. Prod had never seeded the real `free` plan (only `demo_*` plans from a past `seed_demo` run), so every signup got NO subscription — which would break limit math once `FREE_TIER_UNLIMITED` is flipped to `False`. Fix: new mgmt command **`seed_billing`** (`inpa/billing/management/commands/seed_billing.py`) — `get_or_create` the `free` + `plus` Plans (price/limits are CREATE-time defaults ONLY, so later Django-Admin edits survive redeploys) AND **backfill an active `free` Subscription for every user missing one**. Idempotent; added to `render.yaml` startCommand (after `seed_promotion`, runs every deploy). **This is a prerequisite for paid launch** (`FREE_TIER_UNLIMITED=False`). Chose a mgmt command over a data migration because existing billing/admin tests `get_or_create` their own plans and a migration-seeded baseline risked conflicts; command matches the `seed_*` convention. Test `SeedBillingCommandTests`. Prod-verified: public `GET /billing/plans/` now returns `free`+`plus` (was demo-only). Note: the test DB does NOT run seeds, so the signal warning still prints during the test suite only — harmless.
- **2026-07-01 (nav-badge extension + home card swap, PR #47, DEPLOYED):**
  - *Nav badge extension:* after #46 (customers·schedule), added **board·promotion·admin** → all 13 notif types now map to exactly one menu (partition). Added `BOARD_NOTIF_TYPES` (board_comment·board_like), `PROMOTION_NOTIF_TYPES` (promotion_status·promotion_digital_ready — planner-facing), `ADMIN_NOTIF_TYPES` (promotion_digital_requested — admin-facing) in `notifications/models.py`. `unread-count` now returns `{unread_count, customers, schedule, board, promotion, admin}`. FE: `UnreadCount` interface, `app-nav` `badgeFor` helper, `bottom-nav` "더보기" roll-up + sheet-link badges. (analysis·scripts·baseline·manager·home have no inbound notifs → no badge.)
  - *Home card swap:* moved `SelfDiagnosisShare` (free coverage-check link) into the prominent slot next to the calendar, and moved **보유계약 유지현황 (retention donut)** to the right rail. Removed the "안내 문구 복사" button from the 상담 예약 링크 card (→ only "링크 설정", now same compact size as 판촉물); reverted the booking/promo pair from `grid-rows-2 flex-1` back to `space-y-4`. Removed now-unused `copySample`·`SAMPLE_BOOKING_MSG`.
  - *Height fill (#48):* align the 2-B row to the calendar's height — `SelfDiagnosisShare` gains a `fill` prop (h-full flex-col + link row `mt-auto` to sit at the bottom); retention donut gets `flex-1`+h-full to absorb the rail's leftover height (booking/promo stay compact) → the 3 rail cards together = calendar height.
  - *SelfDiagnosisShare copy/layout (#49):* body text 12→14px with per-sentence `<br>` (5 lines, bold kept); bottom restructured into 2 rows — link (one full-width row) + copy/share buttons (row below, split 2-up via flex-1). Shared component used by both home (fill) and customers.
- **2026-07-01 (nav category badges — PR #46, DEPLOYED):** unread badges on the **customers·schedule** sidebar/mobile-tab items too (behaves like the 알림 bell badge). `notifications/unread-count/` returns `{unread_count (total), customers, schedule}` via one group-by. Category constants `CUSTOMER_NOTIF_TYPES` (birthday_soon·share_unread·self_diagnosis_lead) and `SCHEDULE_NOTIF_TYPES` (expiry_soon·consult_reminder·task_due·unpaid_d_alert·meeting_booked) live in `notifications/models.py`. customers/schedule are **subsets of total unread** (the 알림 badge stays the full inbox); cleared by the standard read flow (read / read-all) = "like notifications". FE: extended `getUnreadCount` type, badges in `app-nav` (sidebar) + `bottom-nav` (mobile). Migration 0. Test `UnreadCategoryTests`.
- **2026-07-01 (self-diagnosis /d redesign + dashboard timezone fix — PR #44, DEPLOYED):**
  - *Self-diagnosis (`insurances/self_diagnosis.py` + `app/d/[ref]/page.tsx`):* stronger lead value, consent capture, UX. **Identity now required**: name·phone·birthdate (`birth`=YYYY-MM-DD)·gender (`gender`=1/2); 400 on invalid; phone stored digits-only (dedup also digit-based). **PDF now optional** — with no PDF, skip OCR and just register the lead (`201 {lead_created, analyzed:false}`; FE shows a "상담 신청이 접수됐어요" modal + 나가기/close). Personal-info consent (share to planner) is always required; **overseas transfer (Claude) is required only when a PDF is actually sent** (`consent_overseas_at` is set only then). **New third-party / Inpa-platform-use consent** (`consent_thirdparty` → `ConsentLog.SCOPE_THIRD_PARTY`, customers migration 0015) + marketing = both are legally OPTIONAL (mandatory-consent is prohibited under PIPA); recorded only when checked. FE: identity card (birthdate = 3 dropdowns, gender = 남/여 buttons) + a **"전체 동의하기" (agree-all) button** (toggles all 4) + an **analysis loading screen** (`InpaMark live intense` = red dot pulsing outward like a traffic light + rotating status messages). `InpaMark` gained an `intense` prop (3rd ping ring `.inpa-ping-3`, delays 0/0.57/1.13s). Added `SelfDiagnosisResult.analyzed`. Tests: rewrote `SelfDiagnosisGateTests` (identity required · no-PDF lead · third-party consent) + strengthened existing consent tests.
  - *Dashboard timezone fix (`dashboard/models.py`·`aggregation.py`):* "this month" aggregation dropped to 0 on the UTC/KST month-boundary day. **Unified the window to KST** — `MonthlyGoal.current_month`=`timezone.localtime`, `recent_months`=`timezone.localdate` (previously `timezone.now` (UTC) and `date.today` (OS-local) disagreed with the `__month` ORM lookup, which buckets in `TIME_ZONE`=Asia/Seoul). Migration 0. (See §7 gotcha.)
- **2026-07-01 (home dashboard tweaks — PR #45, DEPLOYED):** **Calendar date click → day-schedule modal** (`app/home/page.tsx` `dayModalOpen`, centered popup, closes on backdrop/✕; calendar itself unchanged). **The "오늘의 일정·할 일" card is now pinned to TODAY** (decoupled from calendar selection; to keep today's items visible even when the user browses other months, it loads the current month separately via `todayScheduleItems`, and a `buildAgenda` helper builds both the calendar (viewed month) and today (current month) maps). `SelfDiagnosisShare` card height bumped (`px-4 py-5`). **상담 예약 + 판촉물 cards equal height** (`grid grid-rows-2` + `h-full`, splitting the rail's leftover height evenly). Migration 0.
- **2026-06-30 (내 소개 카드 + DB 자동 유입):** 판촉물=설계사 소개 실현. `Profile.intro_text`(한줄소개, max 120) + 공개 `IntroductionCardView`(`GET/POST /api/v1/p/<refcode>/`, AllowAny, `accounts/public.py`, ref_code resolve=셀프진단과 동일, config/urls 직접 마운트). GET=설계사 카드(이름·소속·직책·한줄소개)+`/d` 링크. **POST '상담 신청' → 설계사 소유 db 리드 자동 생성**(`lead_source='introduction'`, `consent_overseas_at` 미설정=병력/OCR 아님, `ConsentLog` customer_self·personal_info, phone 중복 재사용, `NotifType.SELF_DIAGNOSIS_LEAD` 알림 재사용). FE: 공개 `app/p/[refcode]`(카드 + '내 보험 무료로 점검받기'(/d) + 상담신청 폼), `components/introduction-card-share.tsx`(판촉물 페이지 상단 `/p` 링크 복사·공유), `settings/account` 한줄소개 입력. 마이그레이션 0010. 테스트 4건.
- **2026-06-30 (관리자 공유 단계 분리):** `manager_share_opt_in`(bool) → **`Profile.manager_share_level`(none/activity/full)** 3단. accounts 마이그레이션 2건(0008 AddField + 0009 백필 opt_in→level). `accounts/manager.py`가 level로 필터(none 제외) + **activity 동의면 per-agent 실적(`premium_month`·`premium_delta`·`retention_y1`)을 None + `shares_performance=False`**, 팀 보험료 합계·유지율·월추이는 **full 동의자만 합산**(+ `totals.perf_agent_count`). ProfileSerializer가 level 저장 시 `manager_share_opt_in` 동기화(레거시 호환). FE: `settings/account` 토글 → **3단 선택**(공유 안 함/활동만/활동+실적), `manager` 순위표에서 `shares_performance=false`면 보험료·유지율 **'비공개'** + 팀 보험료 카드 '실적 공유 N명 기준' 힌트. `seed_demo`·`accounts/tests` 갱신(opt_in→level, 안 하면 매니저 테스트 깨짐). 테스트 5건.
- **2026-06-30 (고객 일괄 등록 + 대시보드 미세조정):** **고객 일괄 등록**(CSV 아님 — 설계사용) — `customers/views.py::CustomerViewSet.bulk_create`(`@action POST /customers/bulk/`, owner 자동주입, 이름 필수·빈행 skip, **(이름+연락처) 중복 건너뛰기**(기존+배치내), `sales_stage='db'`·`lead_source='direct'`, `bulk_create()`). FE `components/customer-bulk-modal.tsx`(붙여넣기 textarea → 줄별 파싱 `01[0-9][- ]?\d{3,4}[- ]?\d{4}` 정규식으로 연락처 추출·나머지=이름 → **미리보기 표** → 'N명 등록'), `app/customers` '여러 명 등록' 버튼. 응답 `{created, skipped}`. 테스트 2건. 마이그레이션 0. **(PR #39 동봉:** 홈 우 레일 상담예약·판촉물 **가로형 카드** + 차트 행 **캘린더↔월별추이 자리 교체**(`lg:order`, 모바일 순서 유지).**)**
- **2026-06-30 (단계 전환율):** 홈(`/home`) 영업 파이프라인 + 관리자(`/manager`) 팀 퍼널에 **단계 전환율(스냅샷)** 표시. `lib/api.ts::funnelConversion(funnel)` = 현재 단계 분포의 **누적 도달**(해당 단계 이상 고객 수)로 DB→TA→FA→청약 3구간 '지금까지 넘어간 비율' 계산(단계 기본값 db 가정). **FE 전용 파생 지표**(스키마·BE·마이그레이션 0). 진단에서 빠졌던 '어디서 막히는지'를 가시화. 정직 라벨('지금까지 넘어간 비율' = 스냅샷, 코호트 추적 아님).
- **2026-06-30 (대시보드 레일 정리 + 어드민 고객 목록):** **홈(`/home`) 우 레일 재배치** — 상단의 셀프진단(`SelfDiagnosisShare`) 위젯을 레일(상담 예약이 있던 자리)로 이동, **상담 예약 카드의 안내문구 미리보기 박스 제거**(복사 버튼·링크 설정은 유지), **상담 예약 + 판촉물 신청을 2열로 나란히·같은 높이**(`grid grid-cols-2 items-stretch` + 각 카드 `flex-col` + `flex-1` 스페이서로 버튼 하단 정렬). **어드민: 설계사별 고객 목록** 신규 — `admin_console`에 `AdminUserCustomersView`(`GET /api/v1/admin/users/<id>/customers/`, IsAdmin) + `AdminCustomerListSerializer`(비민감 필드만: 이름·연락처·단계·상태·직업·보유증권수·등록일·최종연락, **판정어 없음**, 메모·병력·생월일 제외). FE `app/admin/users/[id]` 상세에 '고객 목록(읽기 전용)' 테이블 카드 추가(`adminGetUserCustomers`). 마이그레이션 0. 테스트 1건. **설계사별 사용량**(`/admin/usage`, `AdminUsageView`)은 기존 기능 — 데모(@inpa.local) 제외라 로컬은 비어 보이고 프로드 실사용자에서 집계됨(확인 완료).
- **2026-06-29 (퍼널 다음 + 관리자 툴 — PR #34·#35, DEPLOYED):**
  - *PR #34 (퍼널 다음):* **예약 수락 → FA 자동 승급** — `booking/views.py::accept()`가 고객 `sales_stage`를 db/contact면 meeting으로(이후 단계는 보존). ⚠️ `fa_reached_at`은 `Customer.save()` 훅이 채우므로 `update_fields=['sales_stage','fa_reached_at']`로 함께 영속해야 함(update_fields 함정). **`ContactLog` 신규 모델**(append-only, 결과 5종 부재중·연결·약속·거절·보류 + memo) — `customers/models.py`, `_CustomerScopedViewSet` 재사용, `/customers/<id>/contact-logs/`, 생성 시 `last_contacted_at` 동시 갱신(무접촉 경보 리셋). 마이그레이션 `0014_contactlog`. FE: 고객 상세 '다음 할 일'에 '연락 기록' 버튼 + `ContactLogModal`(결과 5버튼+메모) + 요약카드 '최근 연락' 2건. 테스트 6건.
  - *PR #35 (관리자 조직관리 툴):* `accounts/manager.py` 팀원 루프에 **설계사별 KPI 확장** — `compute_actuals`(이번달 보험료·신규·미팅)·`compute_deltas`(전월대비%)·`compute_funnel`(단계분포)·`agent.last_login`(최근접속, **마이그레이션 0**)·`is_active_month`(신규+미팅>0). 팀 레벨 premium/new 합계·활동멤버수·`team_product_mix`·`team_premium_trend` 추가. `aggregation.py::compute_product_mix(user)` 신규(생명/손해 건수, `portfolio_type=1`). **PII는 그대로 마스킹·집계 수치만**(개별 고객 0건). FE `app/manager/page.tsx` **1장 대시보드 재설계**: 상단 합계 바(StatCard)·팀 퍼널·월별 보험료 추이(BarChart)·상품믹스 막대·유지율/ROI 레일·**설계사별 순위표(실적순, 단계분포 미니바, 활동0 회색+'이번 달 활동 없음' 배지)**, 모바일 카드 스택. `settings/account` 성과 공유 토글 문구를 실제 공유 항목으로 정정. 동의 게이트(`manager_share_opt_in`)·매니저 관계(`Profile.manager` FK) 기존 그대로.
- **2026-06-29 (퍼널 척추 — PR #32, DEPLOYED):** DB→TA→FA→청약 퍼널이 각 단계에서 '다음 영업 행동'을 손에 쥐어주도록. (1) **고객 상세 요약카드 `CustomerSummary`에 단계별 '다음 할 일' 버튼**(전부 기존 자산 재사용): DB=`tel:`/`sms:`+화법(`/scripts?customer=`), TA=예약 링크(`BookingModal`), FA=분석 시작(`setTab('analysis')`)+화법, 청약=체크리스트(`setTab('contract')`). `CustomerSummary`에 `onTab` prop + 자체 `bookingOpen` state 추가. (2) **대시보드(`/home`) 영업 파이프라인 카드 → 단계별 deep-link**: 한 버튼 래퍼 풀고 각 단계 카드를 개별 버튼으로(`router.push('/customers?stage='+key)`). **`/customers`는 `?stage=`를 읽어**(useSearchParams Suspense 회피 위해 `window.location.search` 클라이언트 전용 읽기) `view='kanban'`+`activeStage` 세팅(모바일 탭 선택) + `focusStage` 링 하이라이트(2.2s, 데스크탑). 화법은 `?customer=` 프리필. (3) **고객 대면 링크 '바로 상담 예약' CTA**: `/d` 자가진단 결과·`/s` 공유뷰(죽은 '담당 설계사에게 물어보기' 버튼 → 예약 이동). **BE:** `analytics/views.py::_build_share_payload`에 `booking_url` 조건부 추가(`_booking_url` 헬퍼: `BOOKING_ENABLED`+설계사 `WorkHour` 존재 시에만, `make_booking_token(customer)` 재사용, 요청시점 신선발급 → 72h TTL 무관, 없으면 키 부재 → FE 폴백). **마이그레이션 0**, 회귀 테스트 2건(`ShareViewTests`). `ShareViewResponse`/`SelfDiagnosisResult`에 `booking_url?` 추가.
- **2026-06-29 (모바일 — PR #30, DEPLOYED):** 고객 단계별 보드(`/customers`) **모바일을 가로 스크롤 → 버튼 탭**으로 전환: DB·TA·FA·청약 4버튼(각 단계 인원수) → 탭을 누르면 아래 단일 컬럼이 그 단계로 바뀜(`activeStage` state). **데스크탑은 4단 보드 그대로**(청약 더보기 토글도 데스크탑 전용 `hidden sm:flex`). CSS 분기(브레이크포인트 + `activeStage` show/hide class)라 카드·드래그 마크업 보존, **청약 컬럼은 항상 렌더**(모바일 탭용)하고 데스크탑 노출만 토글 제어(`!showContract`면 `sm:hidden`). 화법(`/scripts`) 치환 입력 4개 `grid-cols-2 → grid-cols-1 sm:grid-cols-2`(모바일 1단), 대시보드(`/home`) 목표 보험료 입력 `w-40 → flex-1 max-w-[160px]`(모바일 넘침 방지).
- **2026-06-29 (design follow-ups — PR #27·#28, DEPLOYED):** 대시보드 컬럼 밸런스(1행 목표↔오늘일정 동일 높이, 2행 좌(통계·파이프라인·차트)↔우 레일 동일 높이·예약 카드가 남는 높이 채움), **계약 유지율·환수 레이더 카드 대시보드에서 제거**, 상담 예약 카드에 **안내문구 복사**(Profile.booking_msg_template 우선, 없으면 기본 샘플), 월별 추이 **3/6개월만**(기본 6)·막대 h-24→h-44(`BarChart heightClass` prop), 콘텐츠 페이지 폭 **대시보드 너비(max-w-[1440px]) 통일**(반응형 2단→1단). **환수 레이더 → '유지 회차 타이머'(`/churn-radar`)**: 연체·미납·환수액(자동 인지 불가, 보험사 전산 권위) 제거, **계약일 기준 납입회차 자동 계산**(`churn.py::_elapsed_period`, 수기 override) + 13/25회차 ±2회 임박만 표시. `_assess`는 이제 `(is_imminent,label,stage,period)` 4-튜플 → `compute_portfolio_breakdown`(도넛)·`ChurnSyncAlertsView` 호출부 동기화. 도넛 라벨 회차 기준(유지안정 25+/정착 25전/초기 13전/회차 미상), 대시보드 유지현황 도넛이 /churn-radar 진입점.
- **2026-06-29 (design refactor — merged PR #25, DEPLOYED):** app-wide visual system per `docs/ui-redesign-spec.md` (presentation-only; data/routing/logic unchanged; 4 brand colors 파랑#2F58DC/초록#6AAC72/노랑#E7B23E/빨강#C73E38 kept; spec's alt hexes ignored per PM). **Nav: top bar → desktop LEFT SIDEBAR** (`components/app-nav.tsx` = `fixed w-60` aside w/ icon nav + profile card; mobile = slim sticky topbar + existing BottomNav). Body shifts via `globals.css` `body:has(.app-sidebar) main{padding-left:17rem}` (15rem sidebar + 2rem gutter; **no per-page edits**). **Tokens centralized in `app/globals.css`** (`@theme inline` + `@utility shadow-card`; added `canvas`/`brand-soft`/`pos·neg·warn`(+soft/ink) aliases derived from the 4 colors. ⚠️ `design/tokens/inpa-tokens.css` is a STALE reference, NOT active). **Dashboard (`app/home`) → 12-col (8+4) glanceable grid:** 목표+달성률 도넛게이지 / 4 StatCards(아이콘 뱃지+큰 숫자+전월대비 증감) / 단계 컬러 파이프라인+화살표 / 막대+캘린더 (좌 8); 오늘일정·유지현황·유지율·예약/판촉 CTA (우 레일 4); 하단 퀵액션 바. **환수 레이더 카드 대시보드에서 제거**(라우트 `/churn-radar`는 유지), 환수위험 stat은 4칸에서 제외. **StatCard**에 옵셔널 `icon`/`tone`(lucide) 추가. **BE:** `dashboard` 시리얼라이저에 computed `deltas`(전월 대비 %, 가산만·무마이그레이션; FE 우선 사용, 추이로 폴백). 서비스 전반 + 공개링크 4종(s/b/c/d — 동의·면책·워터마크 보존) + 어드민 20p(다크) 토큰 통일. 스크린샷 `~/Desktop/inpa-design-shots-v2/`.
- **2026-06-29 (rounds 1–3, this session):**
  - *Customer:* added `status` axis (진행중/보류/휴면/종료) + softened staleness cue; labels 칸반→단계별 / 리스트→목록; **단계별 board shows top-10 per stage + 더보기**; registration modal dropped planner-attested consent boxes (customer-self link only) + consent-aware OCR upload + 수기→직접 입력; **detail page restructured** (정보=default tab, removed from the bar, reached via summary '세부정보' link; 영업단계+상태 one row; summary 최종연락일 D+N; InfoTab 생년월일 dropdowns + live 보험나이 + editable 직업 + '동의 완료').
  - *Booking:* **→ Calendly engine** (`WorkHour` + `availability.py` free-slot generation; buffer; PENDING + accept/decline; 409 conflict; `Notification.meeting`; google push moved to accept; booking-settings + public page rewritten). Migrations `booking 0003`, `accounts 0007`, `notifications 0007`, `customers 0013` (status).
  - *Analysis:* heatmap → **traffic-light** (넉넉 green/적정 yellow/부족 red), **중립 removed** from UI (no-baseline → '기준 설정' CTA), 2-column.
  - *Dashboard/data:* premium-bar amount label moved inside the bar (white); self-diagnosis copy de-jargoned ('리드' removed); home & /schedule calendars unified to 5 categories (알림 removed); **job master → PM's 메리츠 707** + `seed_jobs` sync/prune.
  - *Customer detail (later in session):* summary card holds avatar+name+phone+최종연락 D+N+세부정보 link AND the 영업단계/상태 selectors (one card, selectors centered); InfoTab 생년월일 = year·month·day dropdowns (2:1:1 width, '년' suffix); **per-insurance cards** (분석=보유, 비교=제안 — 보험제목·종류·계약자·피보험자·월보험료·기간, via `listManualInsurances` + serializer now exposes `contractor_name`/`insured_name`); **공백(gap) tab removed** + tab bar no longer h-scrolls; **비교 분석 proposal entry** (제안서 업로드 = OCR with `portfolio_type=2` param / 직접 입력 = manual modal `defaultPortfolioType=2`) + compare table now labels each coverage 추가/삭제/변경/유지. **비교 분석 selection**: planner picks which 보유 + 제안 insurances to compare (checkboxes), `compareCustomer` POSTs `current_ids`/`proposed_ids`, aggregate + diff reflect the selection (`compare.py::_selected_ids`).
  - *Standing redline added:* easy-words · positive-framing · audience-split (§6).
- **2026-06-28 (PRs #8–16):** consent collection (`/c` multi-scope) + 402 upgrade modal (hidden in beta); Round-2 UI (2-row customer card + DotMenu body-portal, monthly-premium chart, script library); 직업급수 707 + `/jobs/search/`; em-dash purge; "이번 달 미팅" = FA-first-reach; copy-tone cleanup; security hardening (rate-limit + DB cache); content protection (`content-guard.tsx`); 판촉물 골격 (`seed_promotion`); admin usage tracking; heatmap badge = held-coverage count ("보유 N개").

## 12. Pending backlog (also memory `qa-audit-backlog`)

- ✅ **담보 사전 피드백 루프(#26) DEPLOYED 2026-07-08** (PR #70). 이제 어드민 검수→admin_verified 별칭이 파싱 룩업에 즉시 반영. 후속: 골든셋 자동 채점(#18)이 붙으면 별칭 승인 전 substring 오라우팅을 사전 차단(현재는 충돌 경고만).
- ✅ **공유(/s) 스냅샷 보관(#27) DEPLOYED 2026-07-08** (PR #71). 발급 시점 /s 화면 불변 기록 + 180일 자동 파기.
- ✅ **골든셋 정확도 게이트(#18) DEPLOYED 2026-07-08** (PR #72). 사전·매처 변경 시 CI 회귀 검사(앵커 11 하드게이트).
- ✅ **Claude 비용 계측(#17) + 로딩 스켈레톤(#14) DEPLOYED 2026-07-08** (PR #73). 호출당 비용/결과 기록 + 공개페이지 브랜드 스켈레톤.
- ✅ **활성화 퍼널 계측(#16) DEPLOYED 2026-07-08** (PR #74). 코호트 퍼널 + UTM + 인증 멈춤 알람. **→ Phase 2 로드맵 전량(#26·#27·#18·#17·#14·#16) 완료.**
- ✅ **사망 담보 정확도 수정 + Manager 요금제 게이트(기본 OFF) DEPLOYED 2026-07-09** (PR #75). 골든 정확도 0.6176→0.6569. Manager 게이트는 `MANAGER_PLAN_GATE_ENABLED` env로 유료 전환 시 flip.
- ✅ **담보 세분류 개별 인식 DEPLOYED 2026-07-09** (PR #77, PM 확정 '각각 별도로'). 소액암/갑상선암/특정암/질병·고도후유장해 라이브 5-way 반영. 새 업로드부터 적용. 남은 세분류 트랩: '특정(소액)암진단비'류 괄호 삽입(§7 (e))은 아직 미해결.
- ✅ **요금표 한도 정합(A) DEPLOYED 2026-07-09** (PR #78). 랜딩 4한도 정합 + 고객 한도 신설(베타 dormant). 무료 고객 한도 = 월 5명(총량 하드캡 미채택).
- ✅ **AI 비교 중립 재정의(③) + AI 신원 마스킹(⑤) DEPLOYED 2026-07-09** (PR #79). 비교 판정 제거·중립 시각화·자유 A/B 비교(BE) + AI 전송 전 신원 마스킹.
- ✅ **④ 비교안내서 설계사 직접 발송 + 제안 vs 제안 자유 선택 UI DEPLOYED 2026-07-09** (PR #80). 설계사가 중립 비교(담보·보험료, verdict·switch_warnings 제외)를 복사해 직접 발송(인파 미발송, §97 회피) + FE 비교탭 자유 A/B 배정 UI(제안 vs 제안·증권 vs 증권). 텍스트 빌더 `lib/compare-export.ts` 분리+카피가드 편입, 라벨 사실 정확성(제안 vs 제안=A안/B안)·미포함 재유입 버그 수정. **불리사항은 설계사가 따로 고지하라고 화면에 명시**. AI 초안(guide_draft)은 COMPARE_AI_ENABLED 닫혀 export 안 됨(중립 사실만).
  - **→ 이걸로 PM AI/컴플라이언스 방향(③④⑤) 완료.** 남은 컴플라이언스 게이트(COMPARE_AI_ENABLED·COMPARE_PUBLISH_ENABLED·ANALYZE_MEDICAL_ENABLED) flip은 CPO 법적 검토 후. ★ 후속 정책 확인(CPO/법무): 설계사가 §97 불리사항(해지손실·면책·이율)을 복사 텍스트가 아니라 **별도로** 고객에게 고지해야 함(화면 명시했으나 실행은 설계사 책임).
- **프리런치 리뷰 백로그 소진 완료.** 남은 것은 전부 PM 콘솔/운영 작업(§ 아래) + 유료 전환 시점 결정(FREE_TIER_UNLIMITED=False, **Manager 게이트 flip = MANAGER_PLAN_GATE_ENABLED=True**, 랜딩 한도 정합). #28(추천 쿠폰)·#30(판촉물 실사진)·#29(유지율)은 각각 유료 라이브·수요·보험사연동 종속으로 deferred.
- **CI 노트:** gitleaks-action@v2 는 repo `.gitleaks.toml` 허용목록을 무시한다(로컬 gitleaks 바이너리는 존중). 테스트 더미 키 오탐은 `KEY='리터럴'` 패턴을 안 만들면 됨(키 이름 없는 변수로 대입). 진짜 키는 gitignore된 `.env`에만. **⚠️ #29 유지율(13/25회차)은 'deferred' 확정**(인파 = 보험사 전산 미연동 → 해약/미납 여부 불가지 → 유지율 공표는 정직성 해자 훼손. PM 2026-07-08 동의. 보험사 API 제휴 전까지 보류). #28(추천 쿠폰)은 유료 모드 라이브 종속, #30(판촉물 실사진)은 deferred.
- ⬜ **자체 블로그/콘텐츠 마케팅(계획, Phase 0 대기):** 전략 SSOT = `docs/superpowers/specs/2026-07-12-content-marketing-strategy.md`. 자체 도메인 `/blog`(B2B 설계사 중심, 토스식 실용 가이드) + **어드민 글 작성 화면(비개발자 발행)** + Article/BlogPosting 스키마(2026-07-12 SEO 토대 재사용). 하이브리드 배포(자체=원본, 인스타·네이버=재가공). 진행 결정 시 페르소나 카운슬→구현 계획→빌드.
- ⬜ **검색·AI 노출 후속(운영):** PR #82로 토대 배포. 남은 PM 콘솔 = 네이버 소유확인(완료 2026-07-12)·사이트맵 제출 / 구글 사이트맵·URL 색인 요청(/faq 색인 확인 2026-07-12). ★ 랜딩 히어로·요금제 '한 달 무료 쿠폰' 문구 미보장(자동발급 미배선) — 유료 전환 전 정합.
- ⬜ OCR remaining: 종합보험 17-22 unmatched coverages — **needs §8 표준 담보 트리 확장 decision** (deferred). Life 변액 company-unknown now classified `'life'` (2026-07-02, no wrong-company guess). 미매칭은 `UnmatchedLog`로 적재되고 어드민 `/admin/normalization` 미매칭 큐에서 수동 매핑 가능(#26). See memory `ocr-coverage-sections`.
- ⬜ At launch: flipping `FREE_TIER_UNLIMITED=False` activates 402 + the upgrade modal (built) + **coupon (built 2026-07-02; `credit.py` expiry-aware)** — verify upgrade-modal copy first.
- ✅ Booking: legacy manual `MeetingSlot` **API removed** (2026-07-02; model + `Meeting.slot` FK + admin kept). The public booking `start_at` flow is automated-test-verified but not browser-smoke-tested.
- ✅ 비교 분석 selection DONE: `compare.py::_respond` accepts `current_ids`/`proposed_ids` (GET query or POST body via `_selected_ids`; absent=all, present-empty=none) → planner checks which 보유/제안 to compare in SwitchTab (`SelectInsRow`, re-compares on toggle), aggregate + 추가/삭제/변경 reflect the selection. `compareCustomer(id, {currentIds, proposedIds})` POSTs when a selection is given.
- ✅ `GapTab` dead code removed (2026-07-02) + unused `HeatmapDetail` import.
- ⬜ Backfill: pre-existing FA/청약 customers have `fa_reached_at=null` (not counted) — fine going forward.
- ✅ **new.inpa.kr cinematic landing: DEPLOYED 2026-07-08** (spec + §11 참조; PR #66 편승 + 리뷰픽스 PR #67, 라이브 확인). 잔여: Vercel 중복 프로젝트 'extraordinary-transformation'(빌드 실패 반복) — 도메인 안 붙어 있으면 삭제, PM 확인 대기.
- ✅ **Manager plan 등록 완료(2026-07-08, billing 0006 + seed_billing)** — 단, **팀 기능을 Manager 플랜 가입자만 쓰게 막는 권한 게이트는 미구현**(현재 팀 기능은 동의 기반 무료) → 유료 전환 전 별도 결정. 랜딩 표기 한도(월 100건 등 시안 문안)와 billing 실제 한도(200/100/200/100)·미구현 한도(고객 수·리포트)는 유료 전환 전 정합 필요.
- ✅ /login resend UX 이관건 해소(2026-07-08, PR #67): 재제출 시 showResend 리셋 — 무관한 오류 배너에 재발송 버튼 잔존하던 문제 수정.

## 13. Docs map (`docs/`)

- **`docs/dev/00-INDEX.md` = dev-docs master map (SSOT entry).** Full route map, doc index, stream↔entity mapping.
- `docs/dev/02-data-model-and-api.md` = data-model SSOT (42 entities + visibility matrix).
- `docs/dev/` streams: 01 architecture · 11 auth · 12 customer/OCR · 13 share · 14·16 compliance/legal · 15 dashboard · 17 boards · 18 mobile · 19 admin · 20 devops · 21 promotion · 22 notifications · 23 billing · 24 landing · 25 deploy guide (PM).
- `docs/superpowers/specs|plans/` = brainstorm specs + implementation plans. `docs/01~07` (root) = business/product planning originals. `docs/_archive-foliio/` = old archive.
- Decision history / session memory: `.claude/projects/.../memory/MEMORY.md` (+ linked: `qa-audit-backlog`, `ocr-coverage-sections`, `sqlite-vs-postgres-seed-trap`, `google-integration-planned`).
