# === 인파 백엔드 의존성 ===
# 핵심 (척추 — 로컬/운영 공통)
Django==4.2.27
djangorestframework==3.16.1
django-environ==0.11.2
django-cors-headers==4.3.1

# DB 드라이버 (운영=PostgreSQL: Neon/Render 무료 티어. 로컬은 SQLite).
# django-environ env.db() 가 DATABASE_URL(postgres://...) 파싱 → psycopg2 사용.
psycopg2-binary==2.9.9

# AI / OCR / 계산 (Phase 1.1 foliio vendoring 시 사용)
anthropic
pdfplumber==0.11.8       # 증권 PDF 텍스트 추출 (claude_parser 선행 — 운영 OCR 필수)
numpy==1.24.4
numpy-financial==1.0.0
python-dateutil
Pillow==10.1.0

# 운영
gunicorn==21.2.0
whitenoise==6.6.0          # prod 정적파일 서빙 (prod.py 미들웨어에서만 활성)
sentry-sdk[django]
